# TASKS (Generated from state.json)

## Engineer
- [x] E-120: Implement a dummy workflow test task to verify the AI-OS task lifecycle. Acceptance: Mark this task as DONE with a summary acknowledging receipt; no source code changes required. | Tier: 1
  Status: DONE 2026-06-03 — Dummy workflow test task #1 received and acknowledged. Task lifecycle verified end-to-end: preflight → sync check (SYNC_PASS) → DONE mark via task-synchronizer-mcp. No source changes (per acceptance).
- [x] E-121: Workflow test task #2. Verify that ai-watch correctly routes this message to Claude's pane. Acceptance: Mark DONE with summary. | Tier: 1
  Status: DONE 2026-06-03 — Workflow test task #2 received in Claude's pane — ai-watch routing confirmed working (Architect's handoff message reached the Engineer pane and triggered execution). No source change …[full in LOG.md]
- [x] E-122: Formalize the ai-watch busy-gate and submission reliability fixes (E-121). Review the recent manual edits to src/bin/ai-watch (version-string regex `^[0-9]+\.[0-9]+\.[0-9]+` and `sleep 0.1` before Enter) for robustness. Ensure these fixes are properly deployed by the installer. Acceptance: Fixes are verified, ai-watch tests are updated to cover these edge cases, and the installer propagation is confirmed. per .ai/blueprints/interactive-bridge.md | Tier: 2
  Status: DONE 2026-06-03 — Formalized E-121 ai-watch fixes per interactive-bridge.md. (1) Version-string ready-heuristic regex ^N.N.N retained + documented (anchored, permits build suffixes, 3 groups avoids ma …[full in LOG.md]
- [x] E-123: Extend `safe-exec-mcp` to strictly enforce deployment and branch-merge sovereignty. Block `git merge`, `git push`, `git pull`, `git branch`, `git rebase` as well as deployment scripts (`ssh`, `rsync`, `scp`, `npm publish`, `docker push`) when `caller_role` is `architect`. Acceptance: Verification tests cover all new banned commands; `src/mcp/safe-exec-mcp/index.js` updated per `.ai/blueprints/sovereignty-hardening.md` §Data Model. | Tier: 2
  Status: DONE 2026-06-04 — safe-exec-mcp branch-merge + deployment sovereignty per sovereignty-hardening.md §Data Model. Extended the E-102 architect filter: caller_role='architect' now [SOVEREIGNTY_BLOCK]s gi …[full in LOG.md]
- [x] E-124: Implement the smart ai-watch delivery model: stateful persistence using a 'delivered' flag, startup backlog drain, single-writer lock, and per-target independent FIFOs per .ai/blueprints/interactive-bridge.md. | Tier: 2
  Status: DONE 2026-06-04 — Smart ai-watch delivery model per reconciled interactive-bridge.md — persistent delivered flag, startup backlog drain, single-writer lock, per-target FIFOs, delivered-aware eviction, …[full in LOG.md]
- [x] E-125: Implement a fail-closed pre-execution gate for 'safe-exec' (THREAT_MODEL T-HITL-004) by integrating 'analyze_command' blocks into the execution flow, replacing the current advisory-only setup. Acceptance: Tests verify blocks are enforced and prevent execution. per .ai/blueprints/sovereignty-hardening.md. | Tier: 3
  Status: DONE 2026-06-04 — Fail-closed pre-execution gate (T-HITL-004): PreToolUse hook + safe-exec --check CLI exit-2-block every Bash BLOCK verdict (advisory→enforcing); single source of truth via extracted …[full in LOG.md]
- [x] E-126: Consume the compiled Context Cache blob as a prompt-prefix during Claude session initialization to implement the §3.2/3.3 prompt-prefix injection harness-level feature per .ai/blueprints/caching.md. | Tier: 2
  Status: DONE 2026-06-04 — Context Cache prompt-prefix injection (caching.md §3 step 3): cache-manager --emit-context CLI + SessionStart hook inject the compiled System Context blob as hookSpecificOutput.addit …[full in LOG.md]
- [x] E-127: Implement bootloader injection for `caller_role` to prevent impersonation per `.ai/blueprints/sovereignty-hardening.md` §Security. Update `_configure_project_claude_settings` and `_configure_project_gemini_settings` in `src/bin/ai` to write `env["AI_OS_CALLER_ROLE"]` as "engineer" and "architect" respectively. Update `src/mcp/safe-exec-mcp/index.js` `analyze_command` handler to prioritize `process.env.AI_OS_CALLER_ROLE` over the agent-provided argument. Acceptance: tests assert that the bootloader env var overrides the MCP argument. | Tier: 2
  Status: DONE 2026-06-05 — Bootloader caller_role injection (sovereignty-hardening.md §Security, anti-impersonation): `ai install` writes env AI_OS_CALLER_ROLE=engineer (claude settings)/architect (gemini sett …[full in LOG.md]
- [x] E-128: Harden the `safe-exec --check` error path. Currently, if the analyzer crashes, it exits with 0 (fail-open-but-loud). Change this to exit 2 (fail-closed) so that an internal analyzer error definitively blocks the execution hook, ensuring true fail-closed security per `.ai/blueprints/sovereignty-hardening.md`. Acceptance: tests verify that a mocked analyzer crash results in exit code 2. | Tier: 3
  Status: DONE 2026-06-05 — safe-exec --check error path is now FAIL-CLOSED (exit 2, was exit 0): an internal analyzer crash blocks execution rather than allowing it, closing the crash-to-bypass vector (T-HITL- …[full in LOG.md]
- [x] E-129: Implement a tamper-resistant role verification mechanism in `safe-exec-mcp` to prevent an Architect session from self-downgrading via environment variable modification (`unset AI_OS_CALLER_ROLE`). Tie the verification to a tamper-resistant source (e.g., cryptographic session ID) per the residual risk identified in `.ai/COMM.md` and `.ai/blueprints/sovereignty-hardening.md` §Security. Acceptance: tests verify that `AI_OS_CALLER_ROLE` modification inside a running session does not bypass the role check. | Tier: 3
  Status: DONE 2026-06-05 — Tamper-resistant role verification (sovereignty-hardening.md §Security): safe-exec --check resolves the role from an HMAC-signed per-session token (~/.ai-os/run/role-<sid>.lock, 0600 …[full in LOG.md]
- [x] E-130: Execute a formal version release using the `release-manager` skill to mark the completion of the Sovereignty Hardening and Interactive Bridge arcs. Aggregate the DONE tasks, bump the package version appropriately (considering the breaking changes to safe-exec blocks), and tag the release in git. Acceptance: `CHANGELOG.md` is updated, `package.json` is bumped, and a signed git tag is created. | Tier: 1
  Status: DONE 2026-06-05 — Formal release v2.0.0 via release-manager skill. package.json version field added (2.0.0); CHANGELOG.md created (Keep-a-Changelog) headlining the Interactive Bridge + Sovereignty Har …[full in LOG.md]
- [x] E-131: Draft the official v2.0.0 GitHub Release notes using the \`gh release create\` CLI command. Extract the v2.0.0 section from \`CHANGELOG.md\` to serve as the release notes body. Acceptance: A GitHub release is published for the \`v2.0.0\` tag with the changelog notes. | Tier: 1
  Status: DONE 2026-06-05 — Published the official v2.0.0 GitHub Release via `gh release create v2.0.0 --verify-tag --notes-file` using the extracted CHANGELOG.md v2.0.0 section (67 lines: arcs, BREAKING change …[full in LOG.md]
- [x] E-132: Move workspace skills from `.gemini/skills/` to `.agents/skills/` per `.ai/blueprints/antigravity-migration.md`. | Tier: 1
  Status: DONE 2026-06-08 — Framework skills relocation .gemini/skills → .agents/skills (antigravity-migration.md): git mv src+workspace; installer (_install_project + ai sync + install-ai-os.sh) emits .agents/ …[full in LOG.md]
- [x] E-133: Extract and update MCP configuration into standalone `mcp_config.json` files, transitioning `httpUrl` to `serverUrl` per `.ai/blueprints/antigravity-migration.md`. | Tier: 2
  Status: DONE 2026-06-08 — Registered agy provider via `ai provider add agy --config-path .agents/mcp_config.json --mcp-key mcpServers` (E-138 adapter); generated .agents/mcp_config.json (25 stdio servers; zer …[full in LOG.md]
- [x] E-134: Run the `agy` conversion and import legacy plugins via `agy plugin import gemini` per `.ai/blueprints/antigravity-migration.md`. | Tier: 2
  Status: DONE 2026-06-08 — agy (v1.0.6) bootstrapped (on PATH) + `agy plugin import gemini` ran clean (No gemini extensions found — no legacy plugins to migrate). End-to-end role→agy handoff verified (resolve_ …[full in LOG.md]
- [x] E-135: Implement `Role Configuration Store` (`.ai/roles.json`) and update `src/bin/ai` to support `ai install` role flag overrides per `.ai/blueprints/role-abstraction.md`. | Tier: 2
  Status: DONE 2026-06-08 — Role Config Store (role-abstraction.md): src/templates/roles.json default (architect=gemini:1, engineer=claude:0); ensure_ai_templates scaffolds .ai/roles.json; `ai install --archite …[full in LOG.md]
- [x] E-136: Refactor `task-synchronizer-mcp` to use semantic targets (`engineer`, `architect`) in `handoff_control` while retaining backwards compatibility, and update `TASKS.md` generation to use provider-agnostic headers per `.ai/blueprints/role-abstraction.md`. | Tier: 2
  Status: DONE 2026-06-08 — Semantic handoff targets + provider-agnostic TASKS.md headers: handoff_control accepts architect/engineer (legacy claude/gemini retained); roleFromOwner() strips '(Provider)' so head …[full in LOG.md]
- [x] E-137: Update `src/bin/ai-watch` to dynamically parse `.ai/roles.json` and map semantic targets to the correct `tmux` pane identifiers per `.ai/blueprints/role-abstraction.md`. | Tier: 2
  Status: DONE 2026-06-08 — ai-watch dynamic role→pane routing: _load_roles_mapping parses .ai/roles.json; resolve_pane derives pane ordinal from the role map with legacy fallback (architect=gemini:1/engineer=c …[full in LOG.md]
- [x] E-138: Implement the **Provider Adapter System** (`.ai/providers.json`) in the bootloader (`src/bin/ai`), allowing seamless registration and configuration of new CLI agents per `.ai/blueprints/role-abstraction.md`. | Tier: 2
  Status: DONE 2026-06-08 — Provider Adapter System (.ai/providers.json): src/templates/providers.json (claude/agy/gemini); ensure_ai_templates scaffolds it; `ai provider add <name> --config-path <p> --mcp-key …[full in LOG.md]
- [x] E-139: Execute a basic workflow validation test to confirm the Engineer environment is active and receiving tasks. Acceptance: Acknowledge receipt by marking this task DONE with a short summary, no code changes required. | Tier: 1
  Status: DONE 2026-06-08 — Workflow validation acknowledged — Engineer environment is active and receiving tasks post-Antigravity-migration. The Interactive Bridge routed this task to the Engineer and it execu …[full in LOG.md]
- [x] E-140: Implement the `ai sync --agents` mapper to extract AI-OS agent metadata and register them as native Antigravity subagents per `.ai/blueprints/native-subagents.md`. | Tier: 2
  Status: DONE 2026-06-08 — `ai sync --agents` mapper (native-subagents.md): src/shared/subagent-mapper.mjs maps .claude/agents+.gemini/agents → .agents/agents/ai-os-*.json define_subagent manifests (delegate v …[full in LOG.md]
- [x] E-141: Relocate procedural workflow files (`digest_updater.md`, `decision_recorder.md` - delete duplicate, `identity_guardian.md`, `aqg-resolver.md`, `review_synthesizer.md`) from `.claude/agents/` and `.gemini/agents/` to `.agents/skills/`. Update any internal references if needed. | Tier: 2
  Status: DONE 2026-06-08 — Relocated 5 procedural workflows from agent dirs → .agents/skills/ (native-subagents taxonomy): digest_updater/identity_guardian/aqg-resolver/review_synthesizer → src/agents/skills/< …[full in LOG.md]
- [x] E-142: Update `src/shared/subagent-mapper.mjs` filtering logic: ensure it only maps files that represent autonomous personas (e.g., `devops_engineer`, critics) to `.agents/agents/`. Ignore files moved to `.agents/skills/` and explicitly filter out any files containing `context: default` or `type: skill` in frontmatter. | Tier: 2
  Status: DONE 2026-06-08 — subagent-mapper.mjs: maps ONLY personas — skips frontmatter context:default/type:skill; FIXED output to per-agent subdir .agents/agents/<name>/agent.json (was flat *.json which agy i …[full in LOG.md]
- [x] E-143: Update `.ai/blueprints/native-subagents.md` and `.ai/blueprints/antigravity-migration.md` to document the strict taxonomy separating AI-OS Skills (in-context procedural workflows) from AI-OS Agents (mapped to Antigravity native subagents), guiding users on how to author new ones for `agy`. | Tier: 1
  Status: DONE 2026-06-08 — Documented Skills-vs-Agents taxonomy: native-subagents.md (new §Taxonomy + authoring-for-agy + corrected Data Model source/target) + antigravity-migration.md (Data Model). Skills = i …[full in LOG.md]
- [x] E-144: Refactor `ensure_ai_templates` in `src/bin/ai` to copy agents from the framework source directly to `.agents/agents/` inside the project workspace instead of `.claude/agents/` and `.gemini/agents/`. This ensures agents are project-scoped and properly discovered by Antigravity during `ai init` per `.ai/blueprints/native-subagents.md`. Ensure that `E-144` and `E-145` are completed alongside this to complete the migration. | Tier: 2
  Status: DONE 2026-06-08 — agy native subagents shipped as an INSTALLED PLUGIN, not loose .agents/agents/ files (empirically: agy registers custom subagents only via plugins). Added src/shared/plugin-builder.m …[full in LOG.md]
- [x] E-145: Self-Learning Activation Arc: Wire W5-T1, W5-T2, and W5-T3 into the live system. Implement `_generate_memory_palace` in `do_sync` (fail-open), invoke `meta_analyst` instinct extraction in `ai-archive`, and implement the `ai-review-proposed-skills` HITL skill (using `approval-mcp`). [Architect ref E-147] | Tier: 3
  Status: DONE 2026-06-09 — Self-learning arc wired: _generate_memory_palace (fail-open) in do_sync, instinct extraction in ai-archive, ai-review-proposed-skills HITL skill (approval-mcp→promoteSkill). Tier-3 r …[full in LOG.md]
- [x] E-146: Broken Agent Refs Cleanup: Retire `gemini_tasks` and `claude_tasks` and update `decision_recorder` references to use `activate_skill` instead of `activate_agent`. Update references in `GEMINI.md`, `contracts/06_AGENT_TEAMS`, and `trigger-audit`. [Architect ref E-148] | Tier: 1
  Status: DONE 2026-06-09 — Retired gemini_tasks/claude_tasks → activate_skill("ai-task"); decision_recorder agent → activate_skill("decision-recorder"). Fixed broken activate_agent targets in GEMINI.md, contra …[full in LOG.md]
- [x] E-147: Resolve `ai-review` name collision: Rename the Architect's audit skill (`src/agents/skills/ai-review`) to `arch-review` to prevent shadowing by the Engineer's `critic-review` skill. Update all internal references. [Architect ref E-149] | Tier: 1
  Status: DONE 2026-06-09 — Renamed Architect audit skill ai-review → arch-review (src + .agents workspace + mirror + gemini command + GEMINI.md triggers); Engineer's ai-review kept. Resolves context-invoker sh …[full in LOG.md]
- [x] E-148: Update taxonomy and documentation tasks (W6-T6, W6-T8, W6-T1, W5-T9). Move `identity_guardian`/`review_synthesizer` to Skills in `agents.md`, specify `security_engineer` as an agent in CLAUDE/GEMINI.md, and resolve auto-author constraints. [Architect ref E-150] | Tier: 2
  Status: DONE 2026-06-09 — Taxonomy/docs: identity_guardian+review_synthesizer Agents→Skills in agents.md (+decision_recorder/claude_tasks retirement notes); security_engineer fixed to activate_agent across CL …[full in LOG.md]
- [x] E-149: Implement Performance & Profiling Architecture (`performance_engineer` agent + `ai-profile` skill) per `.ai/blueprints/performance-profiling.md`. [Architect ref E-151] | Tier: 3
  Status: DONE 2026-06-09 — performance_engineer agent + ai-profile skill (sandbox profiling → OPTIMIZATION_REPORT.md). In agy plugin. performance-mcp server deferred/flagged. Suite 2784/0. [=Architect ref E-151]
- [x] E-150: Implement Database & State Integrity Architecture (`db_architect` agent + `ai-migration` skill) per `.ai/blueprints/database-integrity.md`. [Architect ref E-152] | Tier: 3
  Status: DONE 2026-06-09 — db_architect agent + ai-migration skill (node:sqlite + schema-validator + mandatory DOWN scripts + identity_guardian PII audit). In agy plugin. Suite 2784/0. [=Architect ref E-152]
- [x] E-151: Implement Autonomous Dependency Maintenance (`dependency_manager` agent + `ai-upgrade` skill) per `.ai/blueprints/dependency-maintenance.md`. [Architect ref E-153] | Tier: 3
  Status: DONE 2026-06-09 — dependency_manager agent + ai-upgrade skill (npm+TestSprite+git branch; cannot bypass critic_security/CVE). In agy plugin. Suite 2784/0. [=Architect ref E-153]
- [x] E-152: Implement Automated Incident Response (`sre_responder` agent + `ai-triage` skill) per `.ai/blueprints/automated-incident-response.md`. [Architect ref E-154] | Tier: 3
  Status: DONE 2026-06-09 — sre_responder agent + ai-triage skill (READ-ONLY over incidents.ndjson via incident-aggregate → plans E-## via add_task, never edits code). In agy plugin. Suite 2784/0. [=Architect ref E-154]
- [x] E-153: Implement the Global Telemetry Interceptor and wire it into the MCP transport layer per .ai/blueprints/telemetry-hardening.md. | Tier: 3
  Status: DONE 2026-06-09 — Global Telemetry Interceptor (src/shared/mcp-telemetry.mjs withTelemetry+instrument) rolled out to all 23 MCP servers; Tier-3 adversarial review PASS (findings fixed). Full suite 283 …[full in LOG.md]
- [x] E-154: Refactor error capturing to properly log the ERROR state into telemetry.sqlite per .ai/blueprints/telemetry-hardening.md. | Tier: 3
  Status: DONE 2026-06-09 — Telemetry error capture: interceptor records SUCCESS/ERROR/TIMEOUT; post-tool-use.sh skips mcp__ (server-side dedup); telemetry.mjs TIMEOUT schema + atomic in-place migration (txn + …[full in LOG.md]
- [x] E-155: Fix the task_velocity aggregator so it reliably records metrics at task completion per .ai/blueprints/telemetry-hardening.md. | Tier: 2
  Status: DONE 2026-06-10 — task_velocity aggregator wired to DONE transition (telemetry.mjs recordTaskVelocityForTask). Merged PR #14.
- [x] E-156: Implement .mcp.test.json generation and isolated config loading in tests/run.sh per .ai/blueprints/test-harness-isolation.md. | Tier: 2
  Status: DONE 2026-06-10 — .mcp.test.json generation + MCP_CONFIG_PATH export in tests/run.sh. Merged PR #14.
- [x] E-157: Add robust bash trap cleanup logic to remove test artifacts from run.sh per .ai/blueprints/test-harness-isolation.md. | Tier: 2
  Status: DONE 2026-06-10 — trap cleanup (EXIT/INT/TERM) for test artifacts in run.sh. Merged PR #14.
- [x] E-158: Implement the provider-agnostic `ai handoff <role> [msg]` CLI + shared src/shared/signal-handoff.mjs helper; route task-synchronizer handoff_control through it; add auto-handoff (task-planner Step 7 + ai-task Step 4) and agy-reliable GEMINI.md guidance, per cli-agnostic-handoff. Fixes agy's unreliable MCP-tool invocation by making handoff shell-native. | Tier: 2
  Status: DONE 2026-06-09 — Provider-agnostic `ai handoff` CLI + src/shared/signal-handoff.mjs; handoff_control routes through it; auto-handoff in task-planner Step 7 + ai-task Step 4 + GEMINI.md. Fixes agy's u …[full in LOG.md]
- [x] E-159: Fix Ghost Tool compliance failures: (a) remove 'Skill' from allowed-tools in src/shared/skills/ai-preflight/SKILL.md (and its mirrors), (b) change allowed-tools: [\"Bash\"] to allowed-tools: Bash in src/shared/skills/ai-incident/SKILL.md (and its mirrors). Verify compliance passes locally using ai doctor --compliance. | Tier: 2
  Status: DONE 2026-06-10 — Ghost Tool compliance: removed Skill, [Bash]->Bash; ai doctor --compliance 0 CRITICAL. Merged PR #14.
- [x] E-160: Prevent test hang in interactive environments: update all custom MCP servers (especially src/mcp/blueprint-aligner-mcp/index.js) to check whether they are being executed directly (isMain) before starting the StdioServerTransport and calling server.connect(). This ensures that importing files in unit tests does not block on stdin. | Tier: 2
  Status: DONE 2026-06-10 — isMain guard on all 23 MCP servers via shared is-main.mjs. Merged PR #14.
- [x] E-161: Update src/templates/CLAUDE.md and CLAUDE.md to guide the Engineer on automatically deciding when to invoke a skill (procedural task) or a subagent (persona delegation), with resilient tool selection (native invoke_subagent when running in agy vs. MCP activate_agent/skill) per .ai/blueprints/agent-invocation-robustness.md. | Tier: 2
  Status: DONE 2026-06-10 — CLAUDE.md + template: auto skill/agent selection + resilient invocation. Merged PR #14.
- [x] E-162: Update src/templates/GEMINI.md and GEMINI.md to guide the Architect on automatically deciding when to invoke a skill (procedural task) or a subagent (persona delegation), with resilient tool selection (native invoke_subagent when running in agy vs. MCP activate_agent/skill) per .ai/blueprints/agent-invocation-robustness.md. | Tier: 2
  Status: DONE 2026-06-10 — GEMINI.md + template: auto skill/agent selection + resilient invocation. Merged PR #14.
- [x] E-163: Implement dynamic `mcp__*` tool harvesting in `src/shared/plugin-builder.mjs` by scanning agent markdown files (allowed-tools and body) for `mcp__` prefixes, adding them to the generated `agent.json` `toolNames` array to grant required critic permissions per `.ai/blueprints/agy-subagent-robustness.md`. | Tier: 2
  Status: DONE 2026-06-10 — mcp__* tool harvesting in plugin-builder.mjs; merged PR #15 (ca16611).
- [x] E-164: Implement `import_manifest.json` deduplication in `src/shared/plugin-builder.mjs` (or during `ai sync`), ensuring only a single, unified `ai-os` plugin import is registered, resolving the duplicate registration issue per `.ai/blueprints/agy-subagent-robustness.md`. | Tier: 2
  Status: DONE 2026-06-10 — import_manifest.json deduplication in plugin-builder.mjs. Merged PR #15.
- [x] E-165: Implement serialized OAuth token pre-refresh check in `src/bin/ai` preflight to serialize and execute token refresh before parallel subagents are spawned, preventing write races per `.ai/blueprints/agy-subagent-robustness.md`. | Tier: 2
  Status: DONE 2026-06-10 — serialized OAuth token preflight verify_auth_token in src/bin/ai. Merged PR #15.
- [x] E-166: Correct better-sqlite3 imports and pragma usage to native node:sqlite DatabaseSync in src/claude/agents/db_architect.md and src/shared/skills/ai-migration/SKILL.md. | Tier: 2
  Status: DONE 2026-06-10 — node:sqlite migration in db_architect.md + ai-migration SKILL.md (source + 3 runtime mirrors + regenerated plugin manifest). Replaced better-sqlite3 import with node:sqlite DatabaseS …[full in LOG.md]
- [x] E-167: Add ~/.agents/skills/ and ~/.ai-os/agents/skills/ to SKILL_ROOTS in src/mcp/context-invoker-mcp/index.js. | Tier: 2
  Status: DONE 2026-06-10 — Added ~/.agents/skills and ~/.ai-os/agents/skills to SKILL_ROOTS in context-invoker-mcp. Project .agents/skills already covered (line 48). context_invoker 24/0.
- [x] E-168: Fix { readOnly: true } parameter to { readonly: true } in src/shared/telemetry.mjs. | Tier: 2
  Status: DONE 2026-06-10 — INVALIDATED — verified false positive. node:sqlite uses camelCase readOnly (capital O); lowercase readonly is ignored and defaults to read-write mode. Current code in telemetry.mjs is correct.
- [x] E-169: Extend sovereignty validation to block writes to .ai/blueprints/ in src/mcp/orchestrator-mcp/index.js and critic_arch template. | Tier: 2
  Status: DONE 2026-06-10 — run_review Check 3 now blocks the whole .ai/blueprints/ tree (dir-prefix match) + critic_arch template updated (source/mirror/manifest). Regression-guard tests added. robustness 39/0 …[full in LOG.md]
- [x] E-170: Resolve documentation drift: (a) update CONTRIBUTING.md line 41 to show agents/skills, (b) fix src/bin/ai line 2397 deprecated warning to guide users to arch-review, and (c) update .ai/blueprints/agents.md to retire gemini_tasks and rename ai-review to arch-review. | Tier: 2
  Status: DONE 2026-06-10 — Doc drift fixed: CONTRIBUTING gemini/skills to agents/skills; ai review deprecation now points to arch-review; agents.md retired gemini_tasks and renamed Architect ai-review to arch-review.
- [x] E-171: Update tests/suites/cli_test.sh line 35 to assert against the renamed skill 'skill: arch-review' instead of the legacy 'skill: ai-review'. | Tier: 1
  Status: DONE 2026-06-10 — Updated cli_test.sh:35 assertion from 'skill: ai-review' to 'skill: arch-review' to match the E-170 deprecation-pointer rename. Suite now PASS=18 FAIL=0 (was 17/1).
- [x] E-172: Update src/gemini/agents/ux_reviewer.md and its mirror .gemini/agents/ux_reviewer.md to specify that visual audit verdicts are recorded via the add_stamp tool (D-040 compliance), and rebuild the Antigravity plugin using the plugin builder. | Tier: 1
  Status: DONE 2026-06-10 — ux_reviewer.md (src/gemini + .gemini mirror) Target line now records verdicts via add_stamp (D-040), never direct REVIEWS.md append; regenerated plugin via plugin-builder.mjs (ux_rev …[full in LOG.md]
- [x] E-173: Add a writable check for telemetry.sqlite in src/shared/telemetry.mjs preflight that logs warning messages on failure but fails open. | Tier: 2
  Status: DONE 2026-06-10 — Added _checkWritable() W_OK preflight probe in telemetry.mjs _openDb (+ fail-open _ensureDir); logs telemetry-db-not-writable and never throws. ~/.ai-os mirror synced. telemetry_test …[full in LOG.md]
- [x] E-174: Remove deprecated references to prd_writer from .ai/DIGEST.md to keep documentation accurate. | Tier: 1
  Status: DONE 2026-06-10 — Removed deprecated prd_writer reference from .ai/DIGEST.md Known Risks line 26. Suite 2879/0.
- [x] E-175: Implement deep environment and permission validation in src/bin/ai doctor --env, checking Node version, global binaries, and directory read/write bits. | Tier: 2
  Status: DONE 2026-06-10 — ai doctor --env: _run_env_diagnostics in src/bin/ai — Node 22+, git/gh/tmux/npx, Docker, R/W bits on ~/.ai-os & .ai, state-db write; exits 1 on critical fail. Wires E-176 mcp-tester. …[full in LOG.md]
- [x] E-176: Implement the MCP Connection Tester (src/shared/mcp-tester.mjs) to spawn each registered MCP server, test tools/list connectivity, and integrate it into ai doctor. | Tier: 2
  Status: DONE 2026-06-10 — New src/shared/mcp-tester.mjs: pure-node stdio client spawns each .mcp.json server, initialize+tools/list only (never tools/call), curated env (no token leak), parallel w/ per-server …[full in LOG.md]
- [x] E-177: Refactor the context cache assembler in cache-manager-mcp to automatically compact/prune done task records older than 7 days when the total character count exceeds 20,000 characters. | Tier: 2
  Status: DONE 2026-06-10 — cache-manager assembleContext compaction: when blob >20000 chars, evict stale (>7d) blueprints oldest-first (files never deleted, still tracked). REINTERPRETED: cache holds blueprint …[full in LOG.md]
- [x] E-178: Implement CLI automation wrapper skills (ai-analyze, ai-cluster, ai-sync-verify, ai-topic, ai-dispatch) for the top 5 high-frequency un-wrapped MCP tools identified by the telemetry insights report. Acceptance: The 5 new skills exist in src/shared/skills/ and are integrated into the core workflow. | Tier: 2
  Status: DONE 2026-06-20 — 5 CLI automation wrapper skills for the top-5 high-frequency MCP tools from INSIGHTS.md: ai-analyze→safe-exec analyze_command, ai-sync-verify→verify_markdown_sync, ai-dispatch→orches …[full in LOG.md]
- [x] E-179: Audit the high-failure-rate MCP tools (execute_code, add_topic_seed, report_performance, validate_payload, get_topic_cluster, parse_workspace, confirm_patch) highlighted in the telemetry insights report, and implement fixes or deprecation paths for them. Acceptance: Root causes are investigated and addressed with code fixes or documented deprecation. | Tier: 2
  Status: DONE 2026-06-20 — Audited 7 INSIGHTS "deprecation candidate" tools — root cause (E-168-class): statusForResult booked every isError as ERROR, but ~all flagged failures are EXPECTED rejections (validat …[full in LOG.md]
- [x] E-180: Execute a database data migration for telemetry.sqlite to introduce the REJECTED status dimension for tool executions. Refactor mcp-telemetry.mjs to book _meta.expected_rejection markers as REJECTED instead of SUCCESS to restore usage-friction visibility for the meta_analyst. Update the meta_analyst SQL queries to handle the new status without flagging them as broken tools. Acceptance: Migration is executed safely (use ai-migration/db_architect), _meta.expected_rejection is booked as REJECTED, and meta_analyst SQL queries are updated. per .ai/blueprints/meta-cognition.md. | Tier: 3
  Status: DONE 2026-06-25 — Telemetry REJECTED status (D-049). telemetry.mjs: +REJECTED in enum/CHECK + extended atomic in-code migration (detection keys on REJECTED so 2-/3-value DBs migrate forward); db_archi …[full in LOG.md]
- [x] E-181: Update README.md totally to reflect the current AI-OS v2 architectural state, Native Subagents taxonomy, Interactive Bridge, and Triad configuration per the user request and recent .ai/DIGEST.md updates. Acceptance: README.md accurately describes the v2.0.0+ state, MCP registry, and workflow. | Tier: 1
  Status: DONE 2026-06-25 — README rewritten to current v3.0.0 state: fixed version/test badges, 22 custom + 3 third-party MCP (added code-execution-mcp, mcp-router), new Native Subagents (20-agent) taxonomy se …[full in LOG.md]
- [x] E-182: Remove all deprecated ai-os CLI commands and update outdated text descriptions across the CLI source (e.g. `src/bin/ai`) to align with the current AI-OS v2 framework state. Acceptance: Unused legacy commands are removed, and help texts/descriptions reflect the current capability set. | Tier: 2
  Status: DONE 2026-06-25 — src/bin/ai usage() now documents 'ai watch' (Interactive Bridge, E-115) and reframes migrate-state/mcp-setup as live Recovery commands instead of falsely listing them as Removed; dis …[full in LOG.md]
- [x] E-183: Rename GEMINI.md to ARCHITECT.md and CLAUDE.md to ENGINEER.md in the project root and templates. Update all internal codebase references (src/bin/ai, install-ai-os.sh, plugin-builder.mjs, and shell scripts) to point to the new rulefile names. Ensure the default provider mapping assigns `claude` to Engineer and `agy` to Architect. | Tier: 2
  Status: DONE 2026-06-25 — Decoupled Triad persona (D-050). Renamed CLAUDE.md→ENGINEER.md, GEMINI.md→ARCHITECT.md (root + templates) as canonical rulefiles; CLAUDE.md/GEMINI.md kept as thin @import bootstrap s …[full in LOG.md]
- [x] E-184: Update README.md to reflect the new decoupled rulefile naming (ARCHITECT.md / ENGINEER.md) and the default CLI provider assignments (`claude` = Engineer, `agy` = Architect) per D-050. Remove references to the deprecated Gemini CLI. | Tier: 1
  Status: DONE 2026-06-25 — README updated for the decoupled naming (ARCHITECT.md/ENGINEER.md canonical + CLAUDE.md/GEMINI.md @import shims) and default providers per D-050 (agy=Architect, claude=Engineer): Tri …[full in LOG.md]
- [x] E-185: Refactor telemetry.mjs to extract the hardcoded status enum values (SUCCESS, ERROR, TIMEOUT, REJECTED) into a single DRY source of truth, and derive the CHECK migration sentinel from these values per .ai/DIGEST.md Known Risks. | Tier: 2
  Status: DONE 2026-06-26 — Refactored telemetry.mjs status enum to a single DRY source (STATUS_ORDER); the membership Set, the SQL CHECK clause (STATUS_SQL_IN), and the migration sentinel (STATUS_MIGRATION_SEN …[full in LOG.md]
- [x] E-186: Implement a byte-identity test for generated src/agents/plugin/agents/meta_analyst/agent.json vs the source .md to close the testing gap per .ai/DIGEST.md Known Risks. | Tier: 2
  Status: DONE 2026-06-26 — Added byte-identity test (plugin_builder_test T-7a/b/c): asserts the committed src/agents/plugin/agents/*/agent.json — meta_analyst specifically, plus every persona — is byte-identic …[full in LOG.md]
- [x] E-187: Fix the flaky code_execution Docker e2e tests by updating the base image or implementing a pull retry mechanism, ensuring they pass consistently on cold environments per .ai/DIGEST.md Known Risks. | Tier: 2
  Status: DONE 2026-06-26 — De-flaked code_execution Docker e2e (T-CODEX-S07): pre-pull python:3.12-slim with 3 bounded retries before the e2e block; if the image still can't be made available, skip determinist …[full in LOG.md]
- [x] E-188: Refactor resolver legacy fallback (remove hardcoded 'gemini' fallback if no-roles.json is missing) and rename global rulefiles src/claude/ and src/gemini/ to src/engineer/ and src/architect/ to complete the D-050 Triad persona decoupling. | Tier: 2
  Status: DONE 2026-06-26 — Closed out per D-052. Part 1 (resolver: ai-watch no-roles.json fallback gemini→agy D-050 default) shipped in PR #25. Part 2 (rename src/claude→src/engineer, src/gemini→src/architect) …[full in LOG.md]
- [x] E-189: Update README.md and .ai/blueprints/role-abstraction.md (or equivalent) to explicitly state that src/gemini/ and src/claude/ are provider-specific adapters, not role logic, per D-052. Close out E-188. | Tier: 2
  Status: DONE 2026-06-26 — Documented D-052: src/claude/ and src/gemini/ are provider-specific adapters (vendor CLI config + @import shims), NOT role logic — roles live in ENGINEER.md/ARCHITECT.md, bound via r …[full in LOG.md]
- [x] E-190: Resolve merge conflicts in the PR stack (#25 and #26) and execute a clean bottom-up merge to master. Run a full post-merge suite execution to guarantee master is 100% green before the next major arc. | Tier: 1
  Status: DONE 2026-06-26 — Integrated the PR stack onto master. #23/#24 already merged; #25 was closed (stale GitHub base, not a real conflict). Built a clean integration branch off master and merged f799f16 ( …[full in LOG.md]
- [x] E-191: Replace 'Architect (Gemini)' and hardcoded 'Gemini' references in all Skill & Agent markdown prompts (`.agents/skills/*`, `src/shared/skills/*`, `src/agents/plugin/agents/*`) with 'Architect (Agy)' per .ai/blueprints/role-abstraction.md. Acceptance: All prompts refer to Agy as the Architect. | Tier: 2
  Status: DONE 2026-06-29 — Gemini→Agy persona migration in skill & agent prompts. 'Architect (Gemini)'→'Architect (Agy)', persona shorthand (handoff/sync-state/log/incident/dispatch skills), and Architect-side …[full in LOG.md]
- [x] E-192: Refactor core framework scripts and MCP servers (`src/bin/ai`, `src/mcp/*`, `src/shared/*`) to replace hardcoded 'gemini' fallback logic and references with 'agy' per .ai/blueprints/role-abstraction.md. Acceptance: Core logic uses 'agy' as the default Architect. | Tier: 3
  Status: DONE 2026-06-29 — Gemini→Agy in core scripts/MCP — owner label 'Architect (Gemini)'→'(Agy)' (src/bin/ai OWNER_PATTERNS output label + footer; tool-schemas; task-synchronizer/github-bridge/safe-exec/pr …[full in LOG.md]
- [x] E-193: Update architectural blueprints and system contracts (`.ai/blueprints/*`, `src/contracts/*`, `src/templates/*`) to reflect 'Agy' as the default Architect provider per .ai/blueprints/role-abstraction.md. Acceptance: Blueprints accurately describe Agy as the Architect. | Tier: 1
  Status: DONE 2026-06-30 — Complete
- [x] E-194: Update test suites (`tests/suites/*`) to assert against 'agy' outputs instead of 'gemini' where the framework CLI or behavior has been migrated per .ai/blueprints/role-abstraction.md. Acceptance: Test suite remains 100% green after migration. | Tier: 2
  Status: DONE 2026-06-29 — Test suite stays 100% green (3007/0) after migration. No assertion edits needed (E-188 already migrated default-resolution asserts; owner-label fixtures are provider-agnostic via rol …[full in LOG.md]
- [x] E-195: Update `ensure_ai_templates` in `src/bin/ai` and create `src/templates/AGENTS.md` to automatically scaffold `.agents/AGENTS.md` containing `@ARCHITECT.md`. Acceptance: `ai init` correctly sets up the Antigravity workspace rules to prevent agy from violating the Architect persona. | Tier: 2
  Status: DONE 2026-06-30 — Scaffold .agents/AGENTS.md → @ARCHITECT.md so Antigravity (agy) auto-loads the Architect persona and stays in role (§35). New src/templates/AGENTS.md shim (mirrors GEMINI.md→ARCHITEC …[full in LOG.md]
- [x] E-196: Fix recurring .ai/ working-tree corruptions surfaced via the incident sweep: (1) hooks/stop-hook.sh wrote the generic "auto-stamped by Stop hook" placeholder into DIGEST's curated Recent Changes — guard skips it; (2) bin/ai do_migrate_state rebuilt state.json from the LOSSY TASKS.md view (date-only timestamps→midnight, depends_on stripped) and overwrote rich state on every `ai init`/install in the shared Engineer+Architect working tree — made non-destructive (append-only merge) + do_init drops --force. Regression tests: stop_hook_test.sh + migrate_state_test.sh T-04.10. Full suite 3028/0. | Tier: 2
  Status: DONE 2026-06-30 — Root-caused + fixed two recurring .ai/ working-tree corruptions from the incident sweep. (1) hooks/stop-hook.sh: guard skips the generic "auto-stamped by Stop hook" DIGEST placeholde …[full in LOG.md]
- [x] E-197: Re-point the advisor-mcp A2A Architect bridge from the retired Gemini CLI to `agy --print` (D-050 follow-through; E-192 deliberately KEPT the gemini -p bridge, leaving synchronous ask_architect escalations broken with IneligibleTierError). Swap execFileSync gemini→agy (--print-timeout 90s -p), drop gemini API-key env passthrough (agy uses Antigravity OAuth under $HOME), update docstrings/tool-description/fallback text, bump VERSION 1.1.0. Propagate to ~/.ai-os mirror. Preserve read-only guarantee (no --dangerously-skip-permissions). Acceptance: advisor_mcp_test green; live `agy -p` smoke returns a ruling. | Tier: 2
  Status: DONE 2026-07-01 — advisor-mcp A2A bridge re-pointed gemini→`agy --print` (execFileSync agy [--print-timeout 90s -p]; dropped gemini API-key env; docstrings/tool-desc/fallback→agy; VERSION 1.1.0; ~/.ai …[full in LOG.md]
- [x] E-198: Implement shell-native `ai add-task` CLI primitive (Architect Ruling A / pending D-053, obtained via A2A bridge 2026-07-05). Symmetric to E-158 `ai handoff`: lets agy persist tasks from the Antigravity shell (bypasses the MCP-unreachable deadlock) and survive verify_markdown_sync. CONSTRAINTS (per agy): (1) wrap the EXACT same add_task state logic as task-synchronizer-mcp — no divergent write path, state.sqlite stays single source of truth, no orphaned tasks; (2) stamp caller_role (architect/engineer); (3) respect the sovereignty lock (AI_OS_SOVEREIGNTY_LOCK / single-writer). Route through a shared helper like signal-handoff.mjs. Add tests + propagate to ~/.ai-os mirror. NOTE: architect-owned follow-through (D-053 record + structured-outputs.md §32 amendment documenting ai add-task + ai handoff as authorized shell-native exceptions) is agy's to persist, NOT the Engineer's (§35 sovereignty gate). | Tier: 2
  Status: DONE 2026-07-05 — Shell-native `ai add-task` (Ruling A): shared state-db::addTask is the single write path for both MCP add_task + the new CLI (src/shared/cli-add-task.mjs); bin/ai do_add_task+dispatc …[full in LOG.md]
- [x] E-199: Fix date-rollover time-bomb in tests/suites/token_optimization_test.sh: T-E-108 hardcoded `.ai/archive/stamps-2026-06.json` (lines 78/85), but _rotateToArchive (state-db.js) names the bucket by current UTC year-month via `new Date().toISOString().slice(0,7)`. Passed only during June 2026; fails every PR since the July rollover (repo-wide CI red). Fix: derive month with `date -u +%Y-%m`. Test-only; archive code is correct. Bundled into E-197 branch to unblock its merge. | Tier: 1
  Status: DONE 2026-07-05 — Fixed token_optimization_test.sh date-rollover time-bomb: derive bucket month via `date -u +%Y-%m` (matches _rotateToArchive's `toISOString().slice(0,7)`) instead of hardcoded stamps …[full in LOG.md]
- [x] E-200: Add a handoff completion barrier so the Architect (agy) can't wake the Engineer mid-task-generation. Root cause: agy registers tasks via an async/batch JSON-RPC script, then emits the handoff before it finishes → Engineer wakes to a half-empty queue and plans against missing tasks. Fix: `ai handoff <role> --settle` (signal-handoff.mjs settleTasks) polls the task-table signature (count+maxRowid+done) until it's stable ~2s before emitting; fail-open on DB error; `--settle-timeout N`. do_handoff forwards flags. Guidance: task-planner Step 7 + ai-handoff Step 4 mandate `ai handoff engineer --settle` as the LAST action. Tests: handoff_cli 158.10/158.11 (incl. live concurrent-writer race). Mirror + skill sync. | Tier: 2
  Status: DONE 2026-07-05 — Handoff completion barrier: `ai handoff <role> --settle` polls the task-table signature until stable ~2s before emitting, so agy's async task registration finishes before the Enginee …[full in LOG.md]
- [x] E-201: Revert Architect's unauthorized direct modifications to ~/.gemini/config/mcp_config.json and .agents/skills/ai-seo/SKILL.md to resolve the ANTI-DRIFT incident. | Tier: 1
  Status: DONE 2026-07-06 — Reverted Architect's unauthorized direct edits (ANTI-DRIFT): git-restored the drifted .agents/skills/ai-seo/SKILL.md mirror to clean HEAD. Out-of-repo ~/.gemini/config/mcp_config.jso …[full in LOG.md]
- [x] E-202: Implement the Semrush MCP server in ~/.gemini/config/mcp_config.json and update the ai-seo/SKILL.md to instruct the agent to use Semrush for finding golden keywords. | Tier: 1
  Status: DONE 2026-07-06 — Implemented Semrush integration properly (canonical, not mirror): added Phase 4 Semrush Keyword Optimization + Golden Keywords output block + audit rule to canonical src/agents/skill …[full in LOG.md]
- [x] E-203: Grant ai-seo the mcp__semrush__* tool in its toolNames list
  Status: DONE 2026-07-31 — Granted ai-seo skill the mcp__semrush__* tool via allowed-tools (canonical src/agents/skills/ai-seo/SKILL.md + .agents/ + ~/.ai-os mirrors, byte-identical). Confirmed allow-rule wild …[full in LOG.md]
- [x] E-204: Auto-trigger ai handoff when a new task is created for another role, eliminating manual handoffs
  Status: DONE 2026-07-31 — Auto-handoff on cross-role task creation: `ai add-task` now auto-emits an `ai handoff` bridge signal when the task's execution role (prefix E→engineer / P→architect) differs from the …[full in LOG.md]
- [x] E-205: Harden MCP roundtrip test helper against transient flakes — add a bounded retry (MCP_CLIENT_RETRIES/TIMEOUT env-tunable, default 3× / 10s, 0.25s backoff) to tests/lib/mcp-client.sh::_mcp_send so a cold node-spawn returning an empty tools/list under CPU load is retried instead of misread as "tool not advertised" (exit 3). Root cause of the intermittent advisor_mcp_test "blueprint parameter optional (got exit 3)" false failure seen once in a full 108-suite run (3065/3066). Test infrastructure only; success path unchanged. | Tier: 2
  Status: DONE 2026-08-04 — Added a bounded retry to tests/lib/mcp-client.sh::_mcp_send (MCP_CLIENT_RETRIES=3/MCP_CLIENT_TIMEOUT=10s, 0.25s×attempt backoff); transient empty tools/list under load is retried, no …[full in LOG.md]
- [x] E-206: Add node:test unit suites with Node's built-in coverage (no new deps) for pure-logic modules, starting with src/mcp/shared/state-db.js (DAG validation, dep parsing, ID allocation, role mapping). Wire them into the existing bash runner via a tests/suites/*_test.sh wrapper so they contribute to the master pass/fail count. Establishes the deterministic, coverage-measured unit-test layer recommended alongside the existing black-box behavioral suites. | Tier: 2
  Status: DONE 2026-08-04 — Added tests/unit/state-db.test.mjs (26 node:test assertions, real temp-SQLite) + tests/suites/node_unit_test.sh wrapper wiring node:test into tests/run.sh. 83% line / 76% branch cove …[full in LOG.md]
- [x] E-207: Fix the second load-sensitive flake: managed_agents_sync_hook_test.sh asserts an async debounced cloud-sync's "projection fetch failed" stderr log lands within a blind fixed sleep window (600ms), which misses under CPU load (passes 5/5 in isolation). Replace the blind time.sleep in _call_mcp_capturing_stderr with an adaptive wait that keeps stdin open and polls stderr for a caller-supplied marker, breaking early when it appears (fast common case) with a generous deadline cap under load. Positive-marker cases (S02/S03/S07) pass WAIT_FOR="projection fetch failed"; negative cases (S04/S05, flag-off/add_stamp) keep the full-window wait to confirm the sync does NOT fire. Test-infra only. | Tier: 2
  Status: DONE 2026-08-04 — Fixed managed_agents_sync_hook flake: _call_mcp_capturing_stderr now uses an adaptive stderr poll (WAIT_FOR marker + deadline) keeping stdin open, replacing the blind 600ms sleep tha …[full in LOG.md]
- [x] E-208: Implement per-pane role binding per .ai/blueprints/role-abstraction.md §Same-Provider Triad (D-054): add the 'ai pane <role>' launcher to src/bin/ai (reads roles.json + providers.json 'launch' adapter, sets AI_OS_PANE_ROLE/AI_OS_CALLER_ROLE in the launch env, forwards optional roles.json 'model', pins the tmux pane title to the role before exec); generate .claude/settings.<role>.json overlays from src/claude/ (env only, NO second SessionStart hook); make hooks/session-start.sh mint the E-129 token for ${AI_OS_PANE_ROLE:-$1} and emit '[AI_OS_ROLE] <role>' as the first additionalContext line; add the Role Resolution clause to ENGINEER.md and ARCHITECT.md; extend hooks/pre-tool-use.sh to match Write|Edit and BLOCK paths outside .ai/ and plans/ when the token-minted role is architect. Acceptance: role_config/role_routing/handoff suites green; new tests for the launcher and the gate; LIVE negative test — Write to src/** from an 'ai pane architect' session is BLOCKED; plain 'claude' launch still mints engineer. Sync ~/.ai-os mirrors. | Tier: 3
  Status: DONE 2026-09-04 — Per-pane role binding: ai pane launcher, launch-time role mint, [AI_OS_ROLE] stamp, Role Resolution clause, Write/Edit sovereignty gate. 3 security rounds; SEC_CLEARED scoped to Writ …[full in LOG.md]
- [x] E-209: Re-order resolve_pane in src/bin/ai-watch for semantic targets per .ai/blueprints/interactive-bridge.md §Pane Resolution Precedence (D-054): exact pane-title match → roles.json ordinal (E-122 agent-pane rules unchanged) → fuzzy title → window name. Provider targets keep the E-117 order. Acceptance: add regression assertions reproducing the COMM.md 2026-09-04 G3 case (Engineer pane titled '✳ Engineer and architect separation with Claude', Architect pane '✳ Claude Code', mapping architect:claude:1|engineer:claude:0 → target=architect resolves to the ordinal-1 pane); role_routing_test + handoff suites green; mirror synced. | Tier: 2
  Status: DONE 2026-09-04 — resolve_pane precedence re-ordered for semantic targets per D-054: exact title → roles.json ordinal → fuzzy title → window name; provider targets keep E-117 order. Four passes extrac …[full in LOG.md]
- [x] E-210: Make advisor-mcp ask_architect provider-aware per .ai/blueprints/role-abstraction.md §Same-Provider Triad (D-054): resolve architect.provider from .ai/roles.json and build argv from the providers.json 'print_mode' template ({rulefile}={ARCHITECT.md}, {prompt}); strip 'child_env_unset' vars (CLAUDECODE) from the child env; keep the read-only invariant (print mode, no permission bypass); fallback to agy when roles.json is absent. Extend src/templates providers.json with launch/print_mode/child_env_unset for claude/agy/gemini. Acceptance: advisor_mcp_test covers claude + agy argv shapes and the nested-session env strip; live smoke 'ask_architect' under architect.provider=claude returns a ruling; mirror synced. | Tier: 2
  Status: DONE 2026-09-04 — advisor-mcp ask_architect is provider-aware: resolves architect.provider from roles.json, builds argv from providers.json print_mode, appends ARCHITECT.md for claude, strips CLAUDECO …[full in LOG.md]
- [x] E-211: Deprecate legacy provider targets in src/bin/ai-watch resolve_pane per .ai/blueprints/role-abstraction.md §Same-Provider Triad (D-054): emit a stderr deprecation warning for target 'claude'/'gemini' (removal v4.0) and fail closed (return 1 with hint 'use architect/engineer') when .ai/roles.json maps both roles to that provider. Acceptance: role_routing_test asserts the warning and the same-provider ambiguity path; handoff_control_test green; mirror synced. | Tier: 1
  Status: DONE 2026-09-04 — Legacy provider targets claude/gemini now warn on stderr (removal v4.0) and fail closed with an actionable hint when roles.json maps both roles to that provider. Semantic targets una …[full in LOG.md]
- [x] E-212: Implement the role manifest and role-aware provisioning per .ai/blueprints/architect-provider-parity.md §Components 1-2: add registry.json 'roles' (architect: agents/skills + gemini/agents; engineer: claude/skills + claude/agents; shared: shared/skills); make do_sync in src/bin/ai give each provider dir from .ai/roles.json the shared assets plus every role that provider serves (union, byte-identical, _SKILLS_INDEX regenerated); normalize the 7 src/gemini/agents personas (ux_reviewer, docs-architect, knowledge_architect, meta_analyst, memory_curator, seo_manager, seo_content_generator) to the Claude agent frontmatter contract WITHOUT widening allowed-tools beyond the agy plugin toolNames. Acceptance: with architect=claude, .claude/skills contains the 15 Architect skills and .claude/agents the 7 agents and all load in /agents; with architect=agy the layout is unchanged; sync/skill_context/role_config suites green; ~/.ai-os mirror synced. | Tier: 2
  Status: DONE 2026-09-07 — Role manifest (registry.json roles) + role-manifest.mjs join + role-aware do_sync. architect=claude now gets 47 skills/20 agents; architect=agy unchanged. 7 Architect personas normal …[full in LOG.md]
- [x] E-213: Bring the Architect settings overlay and identity labels to parity per .ai/blueprints/architect-provider-parity.md §Components 3-4: .claude/settings.architect.json adds Bash(ai add-task *), Bash(ai handoff *), mcp__task-synchronizer-mcp__handoff_control/add_topic_seed/add_cluster_page and mcp__context-guardian-mcp__check_role_access allow rules; drop the legacy Bash(gemini -p *) from the base settings template; owner labels in src/shared/cli-add-task.mjs and state-db become 'Architect (<provider>)' / 'Engineer (<provider>)' from .ai/roles.json; hooks/stop-hook.sh stamps 'Actor: <provider> (<role>)'. Acceptance: from an 'ai pane architect' session safe-exec analyze_command reports caller_role=architect (MCP spawn identity parity with agy); cli_add_task + handoff + stop-hook suites green; mirrors synced. | Tier: 2
  Status: DONE 2026-09-07 — Architect overlay gains its allow rules (no deny list — the hooks are the boundary); legacy Bash(gemini -p *) dropped and pruned; owner labels and the Stop hook Actor stamp resolve t …[full in LOG.md]
- [x] E-214: Implement the Architect-scoped Git Lane in hooks/pre-commit.sh per .ai/blueprints/architect-provider-parity.md §Git Lane: read the E-129 session role (role-stamp file first, AI_OS_CALLER_ROLE fallback); when role=architect BLOCK the commit if any staged path is outside .ai/ or plans/ ([SOVEREIGNTY_BLOCK] with a hint to hand the change list to the Engineer); waive the CRITIC_STAMP requirement for architect-scoped commits while keeping the credential scan and the E-122 co-modification warning; Engineer role behaviour unchanged. Dispatch security_engineer (Gate 2 contract change). Acceptance: git_hooks/pre_commit suites cover architect-in-scope PASS, architect-out-of-scope BLOCK, engineer unchanged; live test from the Architect pane; mirror synced. | Tier: 3
  Status: DONE 2026-09-07 — Architect Git Lane: architect commits restricted to .ai//plans/, CRITIC_STAMP waived in scope, other gates intact. Role from the HMAC record via new safe-exec --verify-role. security …[full in LOG.md]
- [x] E-215: Make ARCHITECT.md runtime-aware and add the ai doctor per-role provisioning report per .ai/blueprints/architect-provider-parity.md §Components 5-6: mirror ENGINEER.md's Session Start ladder (Step 1 Skill tool ai-preflight, Step 2 run_preflight, Step 3 activate_skill) and 'use the Skill tool when present' in Skill Invocation; move the Gemini Model Mandate under a 'Provider notes' heading; leave the Forbidden Zone and §35 text unchanged; extend 'ai doctor' in src/bin/ai to print, per role → provider, whether the provider dir holds that role's skills/agents, whether the settings overlay exists, and whether the rulefile is present. Acceptance: GEMINI.md/CLAUDE.md shims unchanged; rulefile + doctor suites green; mirrors synced. | Tier: 2
  Status: DONE 2026-09-07 — ARCHITECT.md gains the three-step Session Start ladder and Skill-tool-when-present; gemini Model Mandate re-homed under Provider notes; Forbidden Zone/§35/shims unchanged. ai doctor …[full in LOG.md]
- [x] E-216: Widen the Architect write gate per D-055 R1 and .ai/blueprints/role-abstraction.md §Security 'Write-gate coverage': (a) add permissions.deny to the generated .claude/settings.architect.json for mcp__filesystem__write_file/edit_file/move_file/create_directory, mcp__patch-mcp__patch_file, mcp__propose-patch-mcp__propose_patch/confirm_patch; (b) extend analyzeSovereignty in src/mcp/safe-exec-mcp/index.js with a write-redirect policy for caller_role=architect: redirections (>, >>, tee), cp/mv/install/ln/sed -i/perl -i/rsync/dd/truncate/git apply/patch with a resolved target outside .ai/ or plans/ → [SOVEREIGNTY_BLOCK]; inline interpreters (python3 -c, node -e, perl -e, ruby -e, bash -c, sh -c, eval) and heredoc-fed interpreters BLOCKED outright for architect with the hint to use Write/Edit; fail closed on unparsable targets; reads unaffected. Update the three code-comment sites and tests to state the new coverage and the remaining residual (exotic encodings, git plumbing, interactive editors). Dispatch security_engineer. Acceptance: safe_exec + sovereignty + role_config suites green with positive/negative cases per form; live test from an ai pane architect session — 'echo x > src/x' BLOCKED, 'echo x > .ai/x' allowed, python3 -c BLOCKED; Engineer role unchanged; mirrors synced. | Tier: 3
  Status: DONE 2026-09-07 — Architect write gate widened to three layers: native Write/Edit (E-208), shell channel via new analyzeArchitectWrites, MCP channel via permissions.deny incl. mcp-router proxy_call. S …[full in LOG.md]
- [x] E-217: Resolve the skill-name collision per D-055 R2 and .ai/blueprints/architect-provider-parity.md §Components 2: rename src/agents/skills/ai-task → arch-task and src/agents/skills/repo-oracle → arch-oracle (E-149 pattern; the Engineer keeps ai-task/repo-oracle); update every reference (ARCHITECT.md, task-planner, blueprint-writer, decision-recorder, _SKILLS_INDEX, agy plugin if it lists skills); promote the E-212 collision guard in src/bin/ai do_sync from a warning to a non-zero exit in a multi-role workspace (single-role keeps role-overrides-shared); also add the missing 'context:' key to src/claude/agents/seo_engineer.md so all 20 agents pass the frontmatter contract. Acceptance: ai sync in this repo reports zero collisions; sync/skill_context/role_config/agent-frontmatter suites green; a synthetic collision makes ai sync exit non-zero only when roles.json maps both roles to one provider; mirrors synced. | Tier: 2
  Status: DONE 2026-09-07 — Architect skills renamed to arch-task/arch-oracle (E-149 pattern); Engineer keeps ai-task/repo-oracle. Collision guard now fatal in multi-role workspaces, inert in single-role. seo_e …[full in LOG.md]
- [x] E-218: Restrict the Git Lane stamp waiver per D-055 R4 and .ai/blueprints/architect-provider-parity.md §Git Lane: in hooks/pre-commit.sh grant the CRITIC_STAMP waiver ONLY when _resolve_commit_role obtained the role from the verified session record (safe-exec --verify-role); the AI_OS_PANE_ROLE / AI_OS_CALLER_ROLE fallbacks may still drive the path-scope BLOCK but never the waiver, so a .ai/-only commit from an unverified session keeps the full stamp requirement. Update THREAT_MODEL.md T-GITLANE-001 item 4 from 'accepted' to 'fixed'. Acceptance: git_lane_test covers verified-architect waiver PASS, env-only architect in-scope → no waiver (stamp required), env-only architect out-of-scope → BLOCK, engineer unchanged; live test from the Architect pane; mirror synced. | Tier: 2
  Status: DONE 2026-09-07 — Git Lane stamp waiver now requires the HMAC-verified session record; env fallbacks restrict but never waive. Closes stamp-free commits to .ai/ (which holds REVIEWS.md) by an env-only …[full in LOG.md]
- [x] E-219: Close T-PATCHMCP-001 per D-056 R1: make patch-mcp roleGuard (and propose-patch-mcp confirm_patch) derive the caller role server-side instead of trusting a volunteered caller_role — order: (1) verified session record via safe-exec --verify-role using the harness session id, (2) the server's spawn-frozen AI_OS_CALLER_ROLE env, (3) no evidence → treat as architect (writes confined to .ai/ and plans/). A self-reported caller_role may only ADD restriction, never lift it. AI_OS_SOVEREIGNTY_LOCK=0 restores the legacy guard. Dispatch security_engineer. Acceptance: patch_mcp + propose_patch suites cover verified-engineer allowed, verified-architect blocked outside scope, no-record+no-env → blocked outside scope, volunteered engineer with verified architect → still blocked; live test from the Architect pane with the overlay deny rules removed; THREAT_MODEL T-PATCHMCP-001 marked fixed; mirrors synced. | Tier: 3
  Status: DONE 2026-09-07 — Caller role derived server-side (verified record → spawn env → architect fail-closed); volunteered role may only add restriction. Closes T-PATCHMCP-001. SEC_CLEARED after one blocked …[full in LOG.md]
- [x] E-220: Implement manifest-scoped pruning in ai sync per D-056 R2: write _SYNC_MANIFEST.json in each target skill/agent dir (.claude/skills, .claude/agents, .agents/skills, ~/.ai-os mirrors) recording every path sync wrote with a content hash; on the next sync delete a path ONLY if it is in the manifest, its hash still matches (unmodified), and it is no longer in the source set — everything else is reported as 'orphan (kept)'; add --prune-known to remove an orphan that is byte-identical to a current canonical skill under another name (the E-217 leftovers .agents/skills/ai-task and repo-oracle). First run with no manifest prunes nothing. Acceptance: sync suite covers write→rename→prune, user-authored skill kept, user-modified synced skill kept and reported, --prune-known removes only byte-identical leftovers, fresh clone in a non-git dir exits 0; mirrors synced. | Tier: 2
  Status: DONE 2026-09-07 — Manifest-scoped pruning in `ai sync` (D-056 R2): _SYNC_MANIFEST.json + sha256 evidence; a path is removed only if sync wrote it, nobody edited it, and it is gone upstream. --prune-kn …[full in LOG.md]
- [x] E-221: Close T-PROPOSEPATCH-001 per D-057 §1: propose_patch stores project_root (its safePath base) plus a PROJECT-RELATIVE path — never an absolute path; confirm_patch re-derives its own project root, requires equality with the stored one ([PROJECT_MISMATCH] otherwise), re-runs safePath on the relative path against its own cwd, and re-derives the role via caller-role.mjs (E-219); legacy pending records holding an absolute path are rejected with a re-propose hint (AI_OS_PATCH_LEGACY=1 accepts them for rollback). Dispatch security_engineer. Acceptance: propose_patch suite covers same-project confirm PASS, cross-project confirm → PROJECT_MISMATCH, relative-path escape → blocked by safePath at confirm, legacy record → rejected, list/preview/reject unchanged; THREAT_MODEL T-PROPOSEPATCH-001 marked fixed; mirrors synced. | Tier: 3
  Status: DONE 2026-09-07 — Patch project boundary (D-057 §1): pending rows carry project_root + a relative path; confirm re-derives, requires equality, re-resolves, re-checks role. Audit also closed a lexical …[full in LOG.md]
- [x] E-222: Make run_review's PATH_TRAVERSAL check context-aware per D-057 §2 (src/mcp/orchestrator-mcp/index.js): keep P0 for /etc/, /root/ and for ../ used in runtime path handling; downgrade to P1 advisory when the ../ is anchored to a script-relative base (__dirname, import.meta.url, self_dir, $0, BASH_SOURCE, AIOS/HOME-rooted mirror paths); put the anchor list in one exported constant with fixture cases in both directions (positives: the src/bin/ai locators; negatives: join(req.path, '../') style); AI_OS_REVIEW_STRICT_TRAVERSAL=1 restores the flat regex. Acceptance: orchestrator/run_review suites cover both directions; a diff containing the src/bin/ai locator idiom passes without P0; mirrors synced. | Tier: 2
  Status: DONE 2026-09-07 — run_review PATH_TRAVERSAL is context-aware (D-057 §2): /etc//root/ and runtime path handling stay P0; a ../ anchored to a script-relative base is P1 advisory, still reported. Anchor …[full in LOG.md]
- [x] E-223: Implement install-first helper locators per D-057 §3: add one shared resolver (shell function in src/bin/ai + an .mjs twin) that resolves helpers from the ~/.ai-os install mirror FIRST and consults the dev tree (<toplevel>/src/...) ONLY when the current repo is the framework clone (AIOS_WORKSPACE equals git toplevel, or the toplevel package.json name is the framework package); AI_OS_LOCATE_DEV=1 restores dev-tree-first for framework development. Replace every per-site git rev-parse --show-toplevel locator chain in src/bin/ai and in all six hooks/*.sh (pre-tool-use, session-start, pre-commit, post-commit, post-tool-use, stop-hook). Dispatch security_engineer. Acceptance: a downstream project containing src/mcp/safe-exec-mcp/index.js and src/shared/<helper>.mjs never has those files executed by any hook or ai subcommand (negative test with a canary that writes a marker); dogfooding in this repo still resolves the dev tree; all hook + locator suites green; mirrors synced. | Tier: 3
  Status: DONE 2026-09-07 — Install-first helper locators (D-057 §3): one shared resolver, dev tree only inside the framework clone, argv-only dev-tree opt-in no env value can forge, bootstrap guard that means …[full in LOG.md]
- [x] E-224: Implement the executable-markdown classifier per D-058 §3: add one shared classifier (src/shared/markdown-exec.mjs) that marks a markdown line as EXECUTABLE when it is a !-prefixed line in a skill/agent file or sits inside a fenced block tagged bash/sh/zsh/js/mjs/javascript/python/node, and DOCUMENTATION otherwise; run_review in src/mcp/orchestrator-mcp/index.js applies PATH_TRAVERSAL (with the E-222 anchor exemptions) only to EXECUTABLE markdown lines and to non-markdown files, keeps HARDCODED_SECRET on every line, and FAILS when a !-line appears in .ai/*.md or .ai/blueprints/*.md (no executable lines there by construction); AI_OS_REVIEW_STRICT_TRAVERSAL=1 grades all lines as code. Acceptance: orchestrator suite covers DECISIONS.md prose quoting join(req.path, '../') → no P0, a SKILL.md !-line with ../ → P0, a bash fence with an E-222 anchor → P1, a !-line in a blueprint → FAIL; mirrors synced. | Tier: 2
  Status: DONE 2026-09-08 — Executable-markdown classifier (D-058 §3): !-lines and executable fences are code, other markdown is prose; PATH_TRAVERSAL applies to code only, a !-line in .ai/ docs is itself a P0. …[full in LOG.md]
- [x] E-225: Close the skills half of T-LOCATOR-001 per D-058 §2: rewrite the four cwd-relative helper chains — src/shared/skills/ai-preflight/SKILL.md lines 18 and 129, ai-insights/SKILL.md line 54 (and its two !-lines importing ./src/shared/telemetry.mjs), ai-review-proposed-skills/SKILL.md line 24 — to source "${HOME}/.ai-os/shared/locate.sh" with AI_OS_LOCATE_UNTRUSTED_ENV=1 and resolve via ai_os_locate <helper>; never a cwd-relative or repo-relative path; sync the .claude/ and .agents/ and ~/.ai-os mirrors byte-identically; add an E-80 standards-checker rule (using the E-226 classifier) that FAILS any EXECUTABLE markdown line in src/**/SKILL.md or src/**/agents/*.md referencing src/ or ./ for execution (AI_OS_STANDARDS_SKIP=skill-locator to bypass). Dispatch security_engineer. Acceptance: negative test — a decoy project containing src/shared/incident-aggregate.mjs with a canary never has it executed by ai-preflight, ai-insights or ai-review-proposed-skills; standards suite covers the new rule both ways; THREAT_MODEL T-LOCATOR-001 skills section marked fixed; all skill suites green. | Tier: 3
  Status: DONE 2026-09-08 — Skills half of T-LOCATOR-001 closed (D-058 §2): five sites resolve via ai_os_locate with the unset -f guard, failing closed on a degraded install; new E-80 rule with a tokenising inv …[full in LOG.md]
- [x] E-226: Close T-PROPOSEPATCH-002 per D-058 §4 in src/mcp/propose-patch-mcp/index.js: preview_patch, list_pending_patches and reject_patch derive the target from project_root + rel_path and require equality with their own project root; on mismatch preview renders the stored diff only with a [FOREIGN_PROJECT] banner and reads no file, reject refuses with [PROJECT_MISMATCH], and list shows own-project rows by default and with all:true shows foreign rows as id + rel_path + project_root basename only (never an absolute path); legacy rows without project_root are listed as legacy and never read; AI_OS_PATCH_LEGACY=1 restores the old behaviour. Acceptance: propose_patch suite covers same-project preview with baseline, foreign preview without file read (canary file outside root is never rendered), foreign reject refused, list default vs all:true shapes, legacy row handling; THREAT_MODEL T-PROPOSEPATCH-002 marked fixed; mirrors synced. | Tier: 2
  Status: DONE 2026-09-08 — T-PROPOSEPATCH-002 closed (D-058 §4): preview/list/reject share one rowScope derivation and require project equality; foreign preview reads no file, foreign reject refuses, list hide …[full in LOG.md]
- [x] E-227: Implement 'ai start' per D-059 and .ai/blueprints/cli-collapse.md §ai start: create or reuse the project's tmux session (default 'aios', window = project basename), derive the pane layout from .ai/roles.json so the role with the lower pane_identifier gets the lower tmux pane_index (default 'triad' layout: engineer left full-height, architect right-top, watcher right-bottom), open each agent pane as an interactive shell and send-keys 'ai pane <role>' after the shell prompt is up, run 'ai watch' in the watcher pane, be idempotent (re-run re-pins titles, starts the watcher only if no lock holder, attaches; never duplicates panes), support --no-watch, --detach, --dry-run, read optional .ai/start.json (session ^[A-Za-z0-9_-]{1,32}$, layout, watch, numeric size percentages — reject anything else), and exit 2 with the manual three-command recipe when tmux is absent. Composes ONLY the installed 'ai pane' and 'ai watch' — never a project-supplied script. Acceptance: a tmux-mocked suite asserts pane order matches pane_identifier for both role orders, idempotent re-run, --no-watch, invalid start.json rejected; a live smoke in a scratch tmux server reproduces the reference layout and ai-watch resolves both roles by exact title; mirrors synced. | Tier: 2
  Status: DONE 2026-09-09 — `ai start` Triad launcher (D-059): composition over `ai pane`/`ai watch`, layout from roles.json pane_identifier order, shell-hosted panes, idempotent, validated start.json, non-tmux …[full in LOG.md]
- [ ] E-228: Finish the 'ai start' surface per D-059: add 'ai start --status' (project panes with role/title/command and watcher lock holder) and 'ai start --kill' (tear down the project window, watcher first, confirm unless --yes); add an 'ai doctor' line reporting start readiness (tmux present, roles mapped, providers on PATH); update README.md and CONTRIBUTING.md tmux workflow docs to point at 'ai start' (closes the cli-collapse 'Tmux Documentation' item) and print an 'ai start' hint at the end of 'ai install'/'ai init'. Acceptance: doctor + start suites green; docs mention the manual fallback recipe; mirrors synced. | Tier: 1
- [x] E-229: Make ai-watch survive 'ai sync' rewriting its own script (observed 2026-09-09: a watcher started 2026-09-04 kept running after the 2026-09-07 mirror update, spammed tmux 'not in a mode' every poll and delivered nothing for 24h — bash reads scripts by byte offset, so an in-place rewrite corrupts the running process). Fix per interactive-bridge.md: (a) ai-watch records its script mtime at start and, between polls, re-execs itself (exec "$0" "$@") when the mtime changes — safe because delivery state is persisted in signal.json; (b) 'ai sync'/'ai install' write the mirror atomically (temp file + rename) so a running bash never reads a half-written file; (c) 'ai start' (E-227) treats a watcher whose script is newer than its process start as stale and restarts it. Acceptance: watch suite covers mtime-triggered re-exec with no double delivery and no lost undelivered entry; sync suite asserts atomic rename; mirrors synced. | Tier: 2
  Status: DONE 2026-09-09 — ai-watch re-execs itself when its script identity changes (sub-second _self_stamp — whole-second stat made it inert for the same-second `ai sync` rewrite it exists to catch); install …[full in LOG.md]
- [ ] E-230: Add CI per D-060 §1: this repo has NO CI configuration although DIGEST claims 'CI pins Node 22' and three sprints asked to 'verify on CI'. Run skill ci_gate first (document in .ai/DEVOPS.md). Add .github/workflows/tests.yml running tests/run.sh on ubuntu-latest with Node 22 (GNU patch 2.7.x present by default) on push + pull_request, with the node:test unit layer (UNIT_COVERAGE=1) as a second job; fail on any suite failure; upload the run log as an artifact; add a README badge. Acceptance: the workflow is green on the first run for master; the E-221 patch driver modes are exercised under GNU patch (add an assertion that prints 'patch --version' in the log); DIGEST Stack line corrected to describe the real CI. | Tier: 2
- [ ] E-231: Close the T-LOCATOR-001 known-uncaught operand class per D-060 §2: extend the E-225 standards rule (and the E-224 classifier's executable-line analysis) to re-tokenise, recursively, the operand of bash -c / sh -c / zsh -c / eval / xargs / heredoc-fed interpreters and pipe-fed 'sh', so 'bash -c "node src/bin/ai"' is caught like a bare 'node src/bin/ai'. Over-block guard is mandatory (D-056 R3 lineage): add fixture cases in BOTH directions before widening, including prose that quotes bash -c, and treat any new over-block as a regression. Acceptance: standards + classifier suites cover each wrapper form positive and negative; the full skill corpus still passes the rule; THREAT_MODEL T-LOCATOR-001 'known uncaught' section marked fixed; mirrors synced. | Tier: 2
- [ ] E-232: Close the skill consent gap per D-060 §3: a !-prefixed auto-executed line in any skill or agent file may only (a) run framework helpers via the installed resolver or (b) run read-only inspection commands (git status/log, reading .ai/ files, tmux/env queries); it may NEVER execute a project-supplied program (tests/run.sh, npm/pnpm/yarn run, make, scripts/*, ./...). Convert src/shared/skills/ai-debug/SKILL.md line 14 and src/claude/skills/bug-reproducer/SKILL.md line 15 (and any other of the six entries recorded in THREAT_MODEL) into explicit numbered steps the agent performs after loading; add the rule to the E-80 standards checker using the E-224 classifier; sync all mirrors byte-identically. Acceptance: no !-line in src/**/SKILL.md or src/**/agents/*.md executes a project path; standards suite covers the rule both ways; ai-debug and bug-reproducer suites green; THREAT_MODEL consent entries marked fixed. | Tier: 2
- [x] E-233: Gitignore the E-220 sync manifests per D-060 §4: add .claude/skills/_SYNC_MANIFEST.json, .claude/agents/_SYNC_MANIFEST.json and .agents/skills/_SYNC_MANIFEST.json (and the ~/.ai-os mirror equivalents where a project .gitignore applies) to .gitignore alongside _SKILLS_INDEX.md; ai init/ai sync append the pattern to a project's .gitignore if missing (idempotent). Acceptance: git status is clean after ai sync in this repo and in a fresh ai init project; sync suite asserts the .gitignore line. | Tier: 1
  Status: DONE 2026-09-09 — `ai init`/`ai sync` add generated-artefact patterns to a project's .gitignore via _ensure_generated_gitignore — idempotent by meaning, no-op outside a git work tree. sync_prune 52/0 …[full in LOG.md]

## Architect
- [x] P-41: Perform a post-migration audit: verify `agy` sign-in, confirm semantic role routing across panes, and validate that the relocated skills are correctly resolved by the `context-invoker`. Acceptance: `agy` is authenticated, `ai-watch` routes correctly, and all tests remain green under the new provider mapping. | Tier: 2
  Status: DONE 2026-06-09 — Post-migration audit completed via Engineer's pre-verified inputs. All tests remain green (2784/0), semantic role routing across panes is confirmed, and relocated skills are resolved …[full in LOG.md]
- [x] P-42: Draft a blueprint for native Antigravity subagent integration to map AI-OS agents to the Antigravity UI. | Tier: 2
  Status: DONE 2026-06-08 — Blueprinted native Antigravity subagent integration to .ai/blueprints/native-subagents.md
