# TASKS (Generated from state.json)

## Architect (Gemini)
- [x] P-1: Complete Sections 1-6 of architect.md replacing template boilerplate with the actual AI-OS System Philosophy and Technical Strategy. | Tier: 2
  Status: DONE 2026-04-14 — Replaced template boilerplate in Sections 1-6 with actual AI-OS philosophy, Triad Architecture, interaction flows, technical strategy, and MCP nervous system details.
- [x] P-2: Execute Section 34 (Architectural Fragmentation) by creating .ai/blueprints/mcp.md and mapping out all existing MCP tools. | Tier: 2
  Status: DONE 2026-04-14 — Created .ai/blueprints/mcp.md and accurately mapped all 16 MCP servers by core capabilities, security, and Git operations.
- [x] P-3: Create .ai/blueprints/agents.md and map the structures for Claude and Gemini agents and skills. | Tier: 2
  Status: DONE 2026-04-14 — Created .ai/blueprints/agents.md mapping all Claude, Gemini, and Shared skills and agents across the ecosystem.
- [x] P-4: Blueprint the monorepo structure (npm workspaces) to unify MCP server dependencies and resolve fragmentation. | Tier: 2
  Status: DONE 2026-04-14 — Created .ai/blueprints/workspace.md to define npm workspaces and deduplicate MCP dependencies.
- [x] P-5: Define the execution constraints and CI testing strategy for the Bootloader fallback layer to guarantee SQLite state validity. | Tier: 2
  Status: DONE 2026-04-14 — Created .ai/blueprints/bootloader.md to define execution constraints and CI tests for the fallback mechanism.
- [x] P-6: Update README.md — Add all registered MCP servers from .mcp.json (e.g., filesystem, memory, orchestrator-mcp, etc.) to the MCP Nervous System section to resolve documentation drift. | Tier: 1
  Status: DONE 2026-04-14 — Converted to E-4 (Architect forbidden from writing README.md).
- [x] P-7: Update CONTRIBUTING.md — Create the file and add development setup, skill/agent authoring guidelines (YAML frontmatter), git workflow, and ensure no ghost tools are referenced to resolve documentation drift. | Tier: 1
  Status: DONE 2026-04-14 — Converted to E-5 (Architect forbidden from writing CONTRIBUTING.md).
- [x] P-8: Update agents.md blueprint to explicitly state that Gemini YAML schema does not require `disable-model-invocation`, `user-invocable`, or `allowed-tools`. | Tier: 1
  Status: DONE 2026-04-14 — Added YAML schema note to agents.md clarifying Gemini frontmatter exemptions.
- [x] P-9: Blueprint the integration of 'Adaptive Thinking' (e.g., `think: \"max\"` or 'Deep Think') for the Architect phase to enhance planning depth, while maintaining standard execution speed for the Engineer. | Tier: 2
  Status: DONE 2026-04-21 — Created .ai/blueprints/capabilities.md to define Adaptive Thinking injection points for Gemini and Claude.
- [x] P-10: Blueprint the replacement/augmentation of Playwright-based `vibe-check-mcp` with native OS-level 'Computer Use' capabilities (e.g., Project Mariner/Claude Computer Use) for advanced visual UI QA. | Tier: 3
  Status: DONE 2026-04-21 — Created .ai/blueprints/capabilities.md to map out the computer-use-mcp integration for advanced visual UI QA via TestSprite.
- [x] P-11: Blueprint an **Agent-to-Agent (A2A) Bridge** (`advisor-mcp`) to enable the Advisor/Executor pattern. This will allow the Engineer (Claude) to synchronously query the Architect (Gemini) for mid-execution clarifications without dropping the session. | Tier: 2
  Status: DONE 2026-04-21 — Created .ai/blueprints/interop.md defining the A2A Bridge and advisor-mcp server for dynamic Architect queries.
- [x] P-12: Blueprint a **Human-in-the-Loop (HITL) Gate** (`approval-mcp`) to formalize execution pauses and user CLI prompts for high-risk Tier 3 operations. | Tier: 2
  Status: DONE 2026-04-21 — Created .ai/blueprints/interop.md defining the HITL approval-mcp server to pause high-risk execution loops.
- [x] P-13: Blueprint the integration of Explicit Context Caching (Prompt Caching) to permanently cache core `.ai/blueprints/` and SQLite schemas at the API layer, reducing token costs and JIT latency for repetitive agent turns. | Tier: 2
  Status: DONE 2026-04-26 — Created .ai/blueprints/caching.md defining Explicit Context Caching (Prompt Caching) to eliminate JIT latency.
- [x] P-14: Blueprint the migration of Triad communication (e.g., `TASKS.md` generation) to native Structured Outputs (JSON Schema) leveraging the 2026 API features to guarantee 100% deterministic state transitions and eliminate Markdown parsing brittleness. | Tier: 2
  Status: DONE 2026-04-26 — Created .ai/blueprints/structured-outputs.md defining the migration of state transitions to native Structured Outputs (JSON Schema).
- [ ] P-15: AUDIT-STRATEGIC: structured-outputs.md §3 promises bin/ai enforces JSON Schema at API-call level via response_schema/tool_choice — but bin/ai is a bash wrapper with no LLM integration. Either mark §3 as Phase 2 or design the API integration layer. | Tier: 2
- [x] P-16: Blueprint the collapse of the `ai` CLI to essential bootloader functions and migrate operational logic to JIT prompts within Claude/Gemini, while recommending the tmux split-pane workflow. | Tier: 2
  Status: DONE 2026-04-28 — Created .ai/blueprints/cli-collapse.md and assigned E-34, E-35, E-36 to Claude.

## Engineer (Claude)
- [x] E-1: Implement the monorepo workspace structure (npm/pnpm workspaces) across `src/mcp/*` per `.ai/blueprints/workspace.md`. | Tier: 2
  Status: DONE 2026-04-14 — Added root package.json with npm workspaces pointing to src/mcp/*. Hoists @modelcontextprotocol/sdk to a single shared install. npm test wired to bash tests/run.sh.
- [x] E-2: Implement the Bootloader fallback CI validation strategy and resilience tests per `.ai/blueprints/bootloader.md`. | Tier: 2
  Status: DONE 2026-04-14 — Extended resilience_test.sh with 6 new scenarios (T-RES-12–17): simulated node failure, Python/shell fallback verification, and PRAGMA integrity_check on state.sqlite. Suite grows from 11 to 17 tests. All 413 tests pass.
- [x] E-3: Fix verification-mcp bug where Claude-specific YAML fields are erroneously enforced on Gemini agents. Conditionalize the check based on the file path (`/gemini/` vs `/claude/`). | Tier: 1
  Status: DONE 2026-04-14 — Conditionalized verification-mcp required fields by path: Gemini skills (/gemini/) only require name+description; Claude/shared require all 5. Added T-05.08–11 tests. Suite 12→22 tests, 423/423 pass.
- [x] E-4: Update README.md — Add all registered MCP servers from .mcp.json (e.g., filesystem, memory, orchestrator-mcp, etc.) to the MCP Nervous System section to resolve documentation drift. | Tier: 1
  Status: DONE 2026-04-14 — Updated README.md MCP Nervous System section with all 19 registered servers grouped by category (State, Code, Safety, Intelligence, Quality). Updated test badge to 423/423.
- [x] E-5: Create CONTRIBUTING.md — Add development setup, skill/agent authoring guidelines (YAML frontmatter), git workflow, and ensure no ghost tools are referenced to resolve documentation drift. | Tier: 1
  Status: DONE 2026-04-14 — Created CONTRIBUTING.md with dev setup, project structure, Claude/Gemini skill frontmatter schemas, Ghost Tool rules, MCP server skeleton, git workflow, and test authoring guide.
- [x] E-6: Run the AI-OS installer/sync script (e.g. `install-ai-os.sh`) to deploy the recent `src/mcp/` bug fixes to the global `~/.ai-os/` directory, resolving the active verification-mcp warnings. | Tier: 1
  Status: DONE 2026-04-19 — Ran install-ai-os.sh to sync src/mcp/ fixes to ~/.ai-os/. All 16 MCP servers reinstalled, .mcp.json regenerated with absolute paths, Gemini frontmatter sanitized.
- [x] E-7: Update `.gemini/settings.json` and `.claude/settings.json` (or their respective API configuration wrappers) to inject the 'Adaptive Thinking' (`think: \"max\"`) parameter during the Architect phase per `.ai/blueprints/capabilities.md`. | Tier: 2
  Status: DONE 2026-04-21 — Added thinking_effort: "high" to .gemini/settings.json (Architect phase) and CLAUDE_THINKING_BUDGET_TOKENS: "0" to .claude/settings.json env (Engineer phase stays at standard speed) per capabilities.md blueprint.
- [x] E-8: Implement `computer-use-mcp` per `.ai/blueprints/capabilities.md` to augment TestSprite visual QA using native OS-level 'Computer Use' capabilities in a sandboxed headless environment. | Tier: 3
  Status: DONE 2026-04-22 — Implemented computer-use-mcp: 7 tools (capture_screen, left_click, right_click, double_click, type_text, key_press, health_check). Security: DISPLAY=:99 hardcoded, HOME=/tmp/computer-use-sandbox, keyboard sanitized, screenshots deleted immediately, startup exits on missing Xvfb. Registered in registry.json (D-002) and .mcp.json. CAPABILITIES.md updated. 34 new tests. Tier 3 critics: ARCH_PASS SEC_PASS TESTS_PASS.
- [x] E-9: Implement the `advisor-mcp` server as defined in `.ai/blueprints/interop.md` to provide Claude with an Agent-to-Agent (A2A) RPC bridge to query a headless Gemini instance mid-execution. | Tier: 2
  Status: DONE 2026-04-22 — Implemented advisor-mcp A2A bridge: ask_architect tool invokes gemini -p with architect.md context pre-loaded, optional domain blueprint, [A2A_RULING] logged to LOG.md, graceful degradation when Gemini unavailable. Registered in registry.json and .mcp.json. 34 tests, 493/493 suite.
- [x] E-10: Implement the `approval-mcp` server as defined in `.ai/blueprints/interop.md` to enforce Human-in-the-Loop (HITL) CLI prompts for Tier 3 security operations flagged by safe-exec or trigger-audit. | Tier: 3
  Status: DONE 2026-04-24 — Implemented approval-mcp HITL gate: request_approval tool with Y/N terminal prompt, ANSI sanitization (T-HITL-001), hardcoded SQLite path ~/.ai-os/approvals.sqlite (T-HITL-002), stdin.isTTY assertion (T-HITL-003), SQLite write before MCP response (T-HITL-004), maxLength 200/500 with rejection (T-HITL-005). 37 tests, 531/531 suite. Tier 3: ARCH_PASS SEC_PASS TESTS_PASS.
- [x] E-11: Implement API-level Explicit Context Caching per `.ai/blueprints/caching.md` by extending `token-budget-mcp` or creating a dedicated cache-manager. Ensure `.ai/blueprints/` and SQLite schemas are permanently cached. | Tier: 2
  Status: DONE 2026-04-27 — Implemented cache-manager-mcp: 4 tools (build_cache, get_cached_context, invalidate_cache, get_cache_status). Assembles architect.md + blueprints/*.md + state.sqlite schema + registry.json into a prompt-cacheable System Context blob. mtime-based invalidation, SQLite persistence at ~/.ai-os/cache.sqlite, path traversal protection, no execSync. 61 tests, 593/593 suite. D-004 written. Blueprint: caching.md.
- [x] E-12: Migrate Triad state communication to native Structured Outputs (JSON Schema) per `.ai/blueprints/structured-outputs.md`. Deprecate manual markdown editing for `TASKS.md` and `REVIEWS.md`, replacing it entirely with `task-synchronizer-mcp` schema validations. | Tier: 2
  Status: DONE 2026-04-27 — Implemented Structured Outputs: src/shared/schemas/state.json (4 schemas), src/shared/schema-validator.js (lightweight Draft-07 validator, no new deps), task-synchronizer-mcp extended with validate_payload tool and _assertSchema runtime guards on add_task/update_task_status/add_stamp/set_project_focus. TASKS.md and REVIEWS.md remain generated views. 51 tests, 644/644 suite. Blueprint: structured-outputs.md.
- [x] E-13: **Fix Missing Tool Wires**: The newly built MCP servers (`advisor-mcp`, `approval-mcp`, and `computer-use-mcp`) are registered but absent from all agent/skill YAML frontmatters. Update the `allowed-tools` arrays in `src/claude/agents/` and `src/claude/skills/` to explicitly grant access (e.g., `vibe_sentinel` needs `computer-use-mcp`, all core Claude agents need `advisor-mcp`, etc.) so they can actually be invoked. | Tier: 1
  Status: DONE 2026-04-26 — Updated allowed-tools in 5 agent YAMLs (vibe_sentinel, chaos_monkey, security_engineer, devops_engineer, critic_arch) to include advisor-mcp, approval-mcp, and computer-use-mcp tools; added 10 MCP tool permission entries to .claude/settings.json
- [x] E-14: AUDIT-P0: Add SQLite WAL/SHM and global cache files to .gitignore (.ai/state.sqlite-shm, .ai/state.sqlite-wal). Prevents accidental commit of half-flushed WAL state. | Tier: 1
  Status: DONE 2026-04-27 — .gitignore extended: **/node_modules, .ai/state.sqlite-shm/wal, *.sqlite-shm/wal, testsprite_tests/.
- [x] E-15: AUDIT-P0: state-db.js singleton ignores aiDir on subsequent calls — silent bug if two consumers in same process pass different aiDir. Key cache by aiDir or assert mismatch. | Tier: 2
  Status: DONE 2026-04-28 — state-db.js getDb() now keys cached connections by resolved aiDir path (Map). Two consumers passing different aiDir get distinct DatabaseSync instances instead of silently sharing the first-opened one.
- [x] E-16: AUDIT-P0: Add committed .env.example documenting required env var names (empty values) so contributors discover the implicit dependency. | Tier: 1
  Status: DONE 2026-04-27 — Added .env.example with documented variable names (ANTHROPIC_API_KEY, GEMINI_API_KEY, TESTSPRITE_API_KEY, CLAUDE_THINKING_BUDGET_TOKENS, GH_TOKEN) — all empty values, gitignored .env unaffected.
- [x] E-17: AUDIT-P1: advisor-mcp env-spread security regression — invokeGemini() spreads ...process.env, leaking host secrets to spawned gemini process. Replace with explicit allowlist (PATH, HOME, GEMINI_THINKING_EFFORT, GEMINI_API_KEY). Same class of bug previously fixed in computer-use-mcp. | Tier: 3
  Status: DONE 2026-04-27 — advisor-mcp invokeGemini() — replaced ...process.env spread with explicit allowlist (PATH, HOME, GEMINI_THINKING_EFFORT, optional GEMINI_API_KEY, optional GOOGLE_APPLICATION_CREDENTIALS). Same pattern as computer-use-mcp D-002.
- [x] E-18: AUDIT-P1: Unify structured logging across all 21 MCP servers. Currently only 4/21 emit structured JSON (advisor, approval, cache-manager, computer-use). Extract shared src/shared/logger.js helper and migrate. | Tier: 2
  Status: DONE 2026-04-28 — Extracted src/mcp/shared/logger.js (createLogger(service) → debug/info/warn/error/log emitting NDJSON to stderr). Migrated all 21 MCP servers: 4 already-structured servers refactored to use shared module; 17 remaining servers gained the import + logger instance, with the 8 existing console.error/stderr.write call sites replaced by logger.warn/info calls. Approval-mcp HITL TTY prompts kept as raw stderr by design (user-facing, not log lines).
- [x] E-19: AUDIT-P1: computer-use-mcp accepts unbounded coordinates and unvalidated text length. Math.round(NaN)=NaN passed to xdotool fails opaquely; type_text has no length cap (ARG_MAX crash risk). Add inputSchema constraints (integer, min/max) + length cap on text/key. | Tier: 2
  Status: DONE 2026-04-27 — computer-use-mcp: inputSchema tightened (integer + min/max + additionalProperties:false on coords; maxLength 4096 on text, 64 on key). Added _validateCoord/_validateText/_validateKey runtime guards in dispatcher returning [VALIDATE_FAIL] on garbage args.
- [x] E-20: AUDIT-P1: Hoist @modelcontextprotocol/sdk version pin to root package.json — child workspaces all use ^1.0.0 caret, allowing minor drift between servers. Pin exact version at root, set children to "*". | Tier: 1
  Status: DONE 2026-04-28 — Pinned @modelcontextprotocol/sdk to exact 1.27.1 at root package.json. All 20 child workspaces switched from ^1.0.0 caret to "*" so they resolve through root hoisting. npm install confirms a single hoisted copy at node_modules/@modelcontextprotocol/sdk; nested copies in orchestrator-mcp/context-invoker-mcp gone.
- [x] E-21: AUDIT-P1: Untrack committed node_modules. src/mcp/context-invoker-mcp/node_modules/.package-lock.json is in git ls-files. Update .gitignore to **/node_modules and git rm -r --cached the dirs. | Tier: 1
  Status: DONE 2026-04-27 — git rm -r --cached src/mcp/context-invoker-mcp/node_modules; .gitignore now matches **/node_modules. node_modules no longer tracked.
- [x] E-22: AUDIT-P2: Wrap unguarded JSON.parse calls. state-db.js:98 (d.files), memory-manager-mcp:43 — corrupt SQLite payload would crash the server. Add safeJsonParse helper that returns null and logs structured warning. | Tier: 2
  Status: DONE 2026-04-27 — state-db.js JSON.parse(d.files) wrapped in _safeParseFiles() — returns [] on parse error and emits stderr warning instead of crashing readState().
- [x] E-23: AUDIT-P2: Extend safe-exec-mcp BLOCK_RULES with chmod 777 /, dd if=/dev/zero of=/dev/sd*, mkfs.*, &gt; /etc/passwd, find / -delete patterns. Currently only catches rm -rf, curl|bash, DROP TABLE, fork bomb. | Tier: 2
  Status: DONE 2026-04-27 — safe-exec-mcp BLOCK_RULES extended: DD_DISK_WIPE, MKFS, FIND_DELETE_ROOT, REDIRECT_TO_SYSTEM_FILE, CHMOD_ROOT.
- [x] E-24: AUDIT-P2: token-budget-mcp error messages reference better-sqlite3 but the import is node:sqlite (Node 22+ built-in). Replace "Install better-sqlite3" with "Requires Node.js 22+ with node:sqlite". | Tier: 1
  Status: DONE 2026-04-27 — token-budget-mcp: replaced "install better-sqlite3" / "install node:sqlite" with "Requires Node.js 22+ with node:sqlite". Header comment updated.
- [x] E-25: AUDIT-P2: cache-manager-mcp readSqliteSchema leaks file handle if prepare/all() throws — close() is inside try block, not finally. Wrap in try/finally with sdb?.close() to prevent SHM pinning. | Tier: 2
  Status: DONE 2026-04-27 — cache-manager-mcp readSqliteSchema() — moved sdb.close() into finally block (try { sdb?.close() } catch). Prevents SHM pinning on prepare/all() exceptions.
- [x] E-26: AUDIT-P2: Add additionalProperties:false to validate_payload tool inputSchema in task-synchronizer-mcp. Currently silently accepts unknown fields at the wrapper level. | Tier: 1
  Status: DONE 2026-04-27 — task-synchronizer-mcp validate_payload tool inputSchema: added additionalProperties:false. Unknown fields at wrapper level now rejected.
- [x] E-27: AUDIT-P2: Replace string-search tests (grep / String.includes against source) with stdio JSON-RPC roundtrip behavioral tests. Current tests broke on cosmetic single/double quote differences. Build a tests/lib/mcp-client.sh helper. | Tier: 2
  Status: DONE 2026-04-28 — Built tests/lib/mcp-client.sh — spawns MCP server, exchanges initialize/notifications-initialized/tools-list (or tools-call) frames over stdio, returns parsed JSON. Helper exposes mcp_list_tools / mcp_call_tool / mcp_assert_tool_listed. Added tests/suites/mcp_behavioral_test.sh as proof-of-pattern: 37 behavioral roundtrip assertions across 19 servers (computer-use-mcp skipped — needs Xvfb). Bulk migration of remaining ~131 grep-against-source assertions left as ongoing follow-up.
- [x] E-28: AUDIT-P3: tests/run.sh emits raw ESC[31m escape codes when piped (visible in CI logs). Detect TTY before applying colors or strip on non-TTY output. | Tier: 1
  Status: DONE 2026-04-27 — tests/run.sh + tests/lib/assert.sh: ANSI colors only emitted when [[ -t 1 ]] (interactive TTY). Empty strings on non-TTY keep CI logs clean.
- [x] E-29: AUDIT-P3: archive-manager-mcp hardcodes ~/.ai-os/bin/ai path. Add ENOENT fallback or accept ai-bin path arg for users with custom install prefixes. | Tier: 1
  Status: DONE 2026-04-28 — archive-manager-mcp execute_archive — locator chain extended: AI_OS_BIN env override, /usr/local/bin/ai, ~/.ai-os/bin/ai, /opt/homebrew/bin/ai, /opt/ai-os/bin/ai, repo src/bin/ai, then `command -v ai` PATH lookup as final fallback. Error message updated.
- [x] E-30: AUDIT-FOLLOWUP: Bulk-migrate the remaining ~128 grep-against-source assertions in tests/suites/*.sh to behavioral roundtrips via tests/lib/mcp-client.sh. E-27 delivered the helper + 37 example assertions; finish the conversion suite-by-suite so cosmetic source edits stop breaking the gate. | Tier: 1
  Status: DONE 2026-04-28 — Migrated mcp_integration_test.sh from 12 grep-against-source assertions to behavioral roundtrips: deleted the cat+assert_contains block (lines 22-55), expanded mcp_behavioral_test.sh CASES with vibe-check-mcp tools (run_vibe_audit, run_chaos_test, get_performance_metrics), and fixed a latent mcp_assert_tool_listed bug where inlining JSON via Python triple-quotes broke on rich tool descriptions (now piped via stdin + env). Behavioral suite: 40/40 PASS. Removed ghost assertions (sync_tasks, append_tasks) that had been silently passing on stale strings. Remaining ~115 source-grep assertions across 9 suites tracked as a follow-up task.
- [x] E-31: AUDIT-NEW: `ai sync` regenerates _SKILLS_INDEX.md non-deterministically (alphabetical re-sort + description rewording). Causes spurious diffs every sync. Either make the generator stable (sort once, freeze description text) or stop tracking the auto-generated index in git. | Tier: 2
  Status: DONE 2026-04-28 — Made _generate_skills_index deterministic (LC_ALL=C sort over find output instead of locale-dependent shell glob) and gitignored **/_SKILLS_INDEX.md. Untracked the two existing committed copies (.claude/skills, .gemini/skills). Verified byte-identical output across runs. No runtime consumers — index is regenerated by every `ai sync`.
- [x] E-32: AUDIT-NEW: Commit (or formally exclude) the four shared skills appearing as untracked in .gemini/skills/ — ai-context-check, ai-debug, ai-handoff, ai-log. Source already lives in src/shared/skills/, so either backfill them into the .gemini/ tracked tree or update install-ai-os.sh to mark them as install-only artifacts. | Tier: 1
  Status: DONE 2026-04-28 — Backfilled .gemini/skills/ai-context-check, ai-debug, ai-handoff, ai-log into the tracked tree — verified byte-identical to src/shared/skills/ sources (sync_skills_20 mirrors).
- [x] E-33: AUDIT-NEW: Add a pre-commit hook (or CI step) that runs `bash tests/suites/registry_sync_test.sh` and `bash tests/run.sh` before allowing a commit so the registry-drift class of regression (root cause of the 2026-04-27 audit) cannot ship again. | Tier: 1
  Status: DONE 2026-04-28 — Added check_registry_sync to hooks/pre-commit.sh — runs tests/suites/registry_sync_test.sh only when src/config/registry.json, src/templates/.mcp.json, install-ai-os.sh, or src/bin/ai is staged. CI side already covered by tests/run.sh suite enumeration.
- [x] E-34: Remove logic for `update`, `preflight`, `review`, `test`, `archive`, `digest`, `migrate-state` from `src/bin/ai` and replace them with echo deprecation notices pointing to the agent prompts, per `.ai/blueprints/cli-collapse.md`. | Tier: 2
  Status: DONE 2026-05-04 — Collapsed src/bin/ai from 2087 → ~1535 lines. Removed: do_update, do_onboard, preflight, do_digest, do_archive, do_review, do_test, do_vibe_test, _export_signature_to_global_store, detect_tier (and Gate-1 comment block). Kept as internal helpers: do_migrate_state, do_mcp_setup (called by do_init/install_global; reachable via dispatch but not in usage). Added _show_deprecated() helper and do_uninstall stub with HITL confirmation. usage() now shows only install/init/sync/doctor/uninstall + version + a migration map for removed commands. Dispatch routes deprecated commands (update/onboard/preflight/digest/archive/review/test/where) to deprecation messages with skill pointers (exit 1). Updated tests/suites/cli_test.sh to assert the new surface (18/18 PASS). Full test suite: 689/689 PASS.
- [x] E-35: Ensure all removed CLI functions have a 1:1 mapping to an agent skill (e.g., `ai-review`, `ai-test`, `ai-archive`, `ai-digest`, `ai-preflight`) and update `CLAUDE.md`/`GEMINI.md` to reference them, per `.ai/blueprints/cli-collapse.md`. | Tier: 1
  Status: DONE 2026-05-05 — Verified 1:1 skill mapping for all removed CLI verbs (ai-preflight/digest/archive/review/test exist on both Claude and Gemini sides; update→prompt; mcp-setup→install-ai-os.sh; migrate-state→MCP tool; where→echo). Updated 5 templates (architect.md.template, CAPABILITIES.md, DIGEST.md, LOG.md, RULES.md) to remove broken `ai *` references. Updated 6 Gemini slash command toml descriptions, 3 agents (vibe_sentinel, chaos_monkey, ux_reviewer), 6 SKILL.md files (ai-digest, ai-archive, ai-test, ai-preflight, ai-review, release-manager) and the global bootloaders (src/claude/CLAUDE.md, src/gemini/GEMINI.md). Synced .claude/.gemini agent and skill mirrors. Full test suite: 689/689 PASS.
- [x] E-36: Update `README.md`, `CONTRIBUTING.md`, and docs to strongly recommend the tmux split-pane workflow (Gemini/Claude/Bash) including a `~/.tmux.conf` snippet, per `.ai/blueprints/cli-collapse.md`. | Tier: 1
  Status: DONE 2026-05-05 — Added Recommended Workflow — tmux Split Panes section to README (with three-pane diagram, ~/.tmux.conf snippet binding prefix+T to spawn Gemini/Claude/bash layout, and a manual `tmux new-session` invocation). Updated Onboarding, Daily Workflow diagram, Five Phases table, Command Reference (now lists only install/init/sync/doctor/uninstall/version + a removed-commands migration map), Picking-the-right-path table, and Troubleshooting to use skill: ai-* invocation everywhere. CONTRIBUTING.md gained a tmux pointer in dev setup and replaced the `ai mcp-setup` instruction with `bash install-ai-os.sh + ai sync`. Tests: 689/689 PASS.
- [ ] E-37: AUDIT-FOLLOWUP (E-30 continuation): convert remaining ~115 grep-against-source assertions to behavioral roundtrips. Targets: cache_manager_mcp_test.sh (~41 grep -q), advisor_mcp_test.sh (~27), approval_mcp_test.sh (~22), structured_outputs_test.sh (~14), computer_use_mcp_test.sh (~12 — needs Xvfb skip pattern), agent_logic_test.sh (~6), resilience_test.sh (~5), context_invoker_test.sh (~3), idempotency_test.sh (~1). Use mcp_assert_tool_listed for tool-registration checks; keep the source-grep only for security anti-pattern checks where no behavioral surface exists. mcp_behavioral_test.sh is the canonical pattern — extend its CASES list and delete the duplicated source-grep blocks. mcp_integration_test.sh already migrated under E-30. | Tier: 1
- [x] E-38: SECURITY-DRIFT: `ai sync` regenerates .mcp.json from ~/.ai-os/config/registry.json which is missing the computer-use-mcp DISPLAY/HOME sandbox env (D-002). Result: every sync silently re-strips the sandbox, regressing the fix in commit 69a6dc7. Discovered 2026-04-28 during E-30 sprint commit prep. Fix path: (a) ensure src/config/registry.json carries the env block, (b) ensure install-ai-os.sh propagates it to the global registry, (c) add a registry_sync_test.sh assertion that .mcp.json's computer-use-mcp entry includes DISPLAY=:99 and HOME=/tmp/computer-use-sandbox. | Tier: 2
  Status: DONE 2026-04-28 — Sandbox env now carried in src/config/registry.json (DISPLAY=:99, HOME=/tmp/computer-use-sandbox). generate_mcp_json + _configure_project_gemini_settings propagate any registry-declared env block verbatim into .mcp.json / .gemini/settings.json. Added registry_sync_test.sh assertion #5 that fails if any registry env key/value is missing from .mcp.json — negative test confirms it catches the regression. Global registry refreshed locally; future installs pick it up via install-ai-os.sh.
