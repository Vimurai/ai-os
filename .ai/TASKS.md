# TASKS (Generated from state.json)

## Engineer
- [x] E-235: Move the Playwright browser download out of the default install per D-061 §3: install-ai-os.sh / do_mcp_setup no longer run 'npx playwright install chromium'; add 'ai mcp-setup --browsers' (explicit, bounded by a timeout, prints progress) and make vibe-check-mcp detect a missing browser and return a clear '[BROWSER_MISSING] run: ai mcp-setup --browsers' rejection instead of hanging; AI_OS_INSTALL_BROWSERS=1 restores the old behaviour. Run skill ci_gate and update .github/workflows/test.yml to install browsers in an explicit cached step so the vibe suites still run. Acceptance: a fresh install completes without network access to the Playwright CDN; ci green; doctor reports browser status; DEVOPS.md updated; mirrors synced. | Tier: 2
  Status: DONE 2026-09-09 — Playwright download out of the default install: opt-in via --browsers, watchdog-bounded (not timeout(1), absent on macOS), progress visible, and [BROWSER_MISSING] at first use instea …[full in LOG.md]
- [x] E-236: Add the environment-dependence check per D-061 §4: add the standing question 'does this assertion depend on what the running machine happens to have (installed CLIs, ambient tmux server, node_modules contents, mirror state)?' to src/claude/skills/ai-review/SKILL.md and src/claude/agents/critic_tests.md; add skip_unless_cmd and skip_unless_env helpers to tests/lib/assert.sh that record a SKIP line (counted separately in SUITE_RESULT) instead of a PASS/FAIL; convert the three tests fixed this sprint to use them where a requirement is genuinely optional. Acceptance: harness suite covers SKIP accounting; full suite green locally and on CI; mirrors synced. | Tier: 1
  Status: DONE 2026-09-09 — SKIP is a distinct outcome (helpers + SKIP= appended so old parsers still work); standing environment-dependence question in critic_tests + ai-review with BOTH remedies; mcp_doc_sync …[full in LOG.md]
- [x] E-237: Hot-reload policy modules in long-running MCP servers per D-061 §5: add src/mcp/shared/load-policy.mjs exporting loadPolicy(name) that dynamically imports traversal-policy / markdown-exec / architect-writes / caller-role with a cache-busting query when the module's mtime changed since the last load (E-229 pattern); orchestrator-mcp run_review and the other consumers call it per request instead of a static import; AI_OS_POLICY_HOT_RELOAD=0 restores static imports; 'ai sync' prints a one-line notice naming running servers whose loaded policy is older than the mirror. Acceptance: a test rewrites traversal-policy.mjs while a server is running and the next run_review reflects the change; no double-import leak (module count bounded); orchestrator suite green; mirrors synced. | Tier: 1
  Status: DONE 2026-09-09 — loadPolicy() hot-reloads the four policy modules on mtime change only (busting per call leaks an ESM record per request). Verified END TO END: a live tier-2 run_review now returns P1 …[full in LOG.md]
- [x] E-238: Add the no-stash rule and the conflict-marker gate per D-062 §3 and .ai/blueprints/git-hooks.md §Conflict-Marker Gate: (a) hooks/pre-commit.sh rejects any staged file containing a conflict marker at line start (<<<<<<< , a bare ======= between them, >>>>>>> ) and rejects a staged .ai/*.json that does not parse as JSON, printing the file and line; AI_OS_SKIP_CONFLICT_GATE=1 bypasses; (b) add to ENGINEER.md Core Rules and the commit-crafter skill: .ai/ bookkeeping moves between branches by commit + cherry-pick, never git stash; (c) add the triage rule to ai-debug and critic_tests: after a run of environmental failures a new CI failure is presumed REAL until its log is read. Acceptance: git_hooks suite covers marker-at-line-start BLOCK, backticked marker text in .md ALLOWED, invalid state.json BLOCK, valid ALLOW; the historical 44243bc content is a fixture that must BLOCK; mirrors synced. | Tier: 2
  Status: DONE 2026-09-09 — No-stash bookkeeping rule + presume-real triage + pre-commit conflict-marker and .ai/*.json parse gate. Verified against the REAL incident: replaying 44243bc's blob blocks on both ch …[full in LOG.md]
- [x] E-239: Make performance budgets host-relative per D-062 §2 and .ai/blueprints/test-harness-isolation.md §Performance Budgets: add perf_budget helpers to tests/lib/assert.sh that measure a same-host baseline (bare node spawn for spawn-bound paths, a no-op hook for hook paths) and, when CI=true, enforce the declared absolute budget, otherwise assert elapsed <= k * baseline + slack (defaults k=2, slack=50ms, declared per assertion), printing baseline and elapsed on every run; convert the incident_aggregator ('under 200ms', measured 381ms locally) and telemetry ('hook warm-path under 250ms') assertions and any other absolute budget in the suite; AI_OS_PERF_ABSOLUTE=1 enforces absolute budgets everywhere; add 'absolute budget without a baseline' to the critic_tests checklist. Acceptance: both suites pass locally on the Engineer's machine AND on CI with the absolute budget still enforced there; a synthetic 5x slowdown fails locally; mirrors synced. | Tier: 2
  Status: DONE 2026-09-09 — assert_perf: absolute budget on CI, k*baseline+slack elsewhere, median same-host baseline, both numbers printed every run. Both opposing properties pinned — the real 381ms incident p …[full in LOG.md]
- [x] E-240: Implement leaked-state hygiene per D-063 §2 and .ai/blueprints/test-harness-isolation.md §Leaked External State: (a) add register_cleanup to tests/lib/assert.sh that installs an EXIT trap and accumulates cleanup commands, and convert every suite that creates external state (tmux servers/sockets, temp dirs outside the sandbox, background processes, .ai/ lock dirs, ~/.ai-os mirror writes, .ai/signal.json entries) to call it BEFORE creating the state, with names from mktemp entropy never $$; (b) tests/run.sh snapshots before and diffs after the run the tmux servers matching the test socket prefix, processes whose argv names the harness sandbox, lock dirs under .ai/, and temp dirs under the harness root, prints 'LEAKED n <kind>' per suite, fails the run when CI=true, and offers --sweep locally; AI_OS_TEST_NO_SWEEP=1 disables the sweep; (c) add review question #3 'what does this test leave behind when an assertion fails halfway?' to critic_tests and ai-review. Acceptance: a fixture suite that fails mid-way leaves nothing behind; a deliberately leaking fixture is reported as LEAKED and fails under CI=true; the sweep never touches a tmux server outside the test prefix (negative test with a foreign socket); full suite green locally and on CI; mirrors synced. | Tier: 2
  Status: DONE 2026-09-09 — Leaked-state hygiene: register_cleanup BEFORE creating state (a trap after the resource has a leak window, and that is where failures happen), mktemp names never $$, runner diffs per …[full in LOG.md]
- [x] E-241: Close the trap-replacement limitation per D-064 and .ai/blueprints/test-harness-isolation.md §Trap Chaining: (a) tests/lib/assert.sh shadows the trap builtin for EXIT only — 'trap <cmd> EXIT' appends <cmd> to the register_cleanup registry instead of replacing the handler, every other signal/argument shape passes to builtin trap, 'builtin trap - EXIT' still clears; handlers run LIFO each in its own subshell so one failing cleanup cannot skip the next; print 'CLEANUP n handlers' on exit; AI_OS_TEST_NO_TRAP_CHAIN=1 restores the plain builtin; (b) document on_exit <cmd> as the spelling and add an E-80 standards rule (tests/** scope, P1) flagging a raw 'trap ... EXIT' in a suite; (c) mechanically convert the 46 existing 'trap ... EXIT' sites in tests/suites/*.sh to on_exit; (d) add review question #4 'is this helper ever called inside a command substitution, a pipeline, or a while-read loop, and does it set state that must outlive that call?' to critic_tests, ai-review and ai-debug, and the D-064 §2 subshell-state rule to the engineering-standards references they cite. Acceptance: a fixture suite that installs its own trap AFTER sourcing assert.sh still self-cleans; the full run reports LEAKED 0 and CLEANUP counts; standards suite covers the new rule both ways; CI green; mirrors synced. | Tier: 2
  Status: DONE 2026-09-09 — assert.sh shadows `trap` for EXIT only so it CHAINS instead of replacing; LIFO handlers each in a subshell; CLEANUP n printed; 42 raw sites converted to on_exit; E-80 rule flags raw …[full in LOG.md]
- [x] E-242: Fix the 'ai start' failure 'Settings file not found: .claude/settings.architect.json' per D-066 §2: provider-adapter.mjs / resolve-launch.mjs substitute an ABSOLUTE overlay path rooted at the .ai/ parent instead of the cwd-relative '.claude/settings.{role}.json'; 'ai pane <role>' generates a missing overlay from roles.json before exec (reuse _write_role_settings_overlays, idempotent) and 'ai start' verifies each role's overlay before send-keys, printing which file it healed; forward roles.json 'model' as today. Acceptance: launching 'ai pane architect' from a project subdirectory and from a cwd outside the project both resolve the overlay; a project with no overlay gets one generated and launches; role_config/pane/start suites green; mirrors synced. | Tier: 2
  Status: DONE 2026-09-10 — Launch path operands anchored at the resolve-launch seam (fixes projects with a pre-existing providers.json too, which were the failing ones). The rulefile was relative as well — a S …[full in LOG.md]
- [x] E-243: Make the all-Claude Triad the default per D-066 §1/§3: src/templates/roles.json and _write_roles_json default to architect=claude:1 model fable, engineer=claude:0 model opus (agy/gemini remain selectable via --architect/--engineer); ai init / ai install / ai sync banners print 'Architect (claude · fable)' / 'Engineer (claude · opus)' derived from roles.json (provider + model); replace the 'Architect (Agy)' owner regex (src/bin/ai ~1657), the 'Agy (Architect)' footer (~1927) and every remaining vendor literal in user-facing text with role-derived labels; update ARCHITECT.md header ('defaults to the claude provider, model fable'), ENGINEER.md §35 redirect (name the Architect by role), README and CONTRIBUTING (all-Claude default, agy/gemini as alternative providers; GEMINI.md/CLAUDE.md shims stay per D-051). Acceptance: fresh 'ai init' in a temp dir shows the Claude/Claude banner with models; role_config + banner + docs suites green; mirrors synced. | Tier: 2
  Status: DONE 2026-09-10 — All-Claude Triad is the default (architect claude:1/fable, engineer claude:0/opus); banner, task-owner regex and review footer now DERIVE the label from roles.json instead of hard-co …[full in LOG.md]
- [x] E-244: Provider workspace hygiene per D-066 §4: 'ai sync' provisions .claude/ .agents/ .gemini/ only for providers mapped in .ai/roles.json (.claude/ always when hooks need it); 'ai doctor' reports an unmapped provider's workspace dir as 'stale provider workspace'; add 'ai sync --prune-providers' that removes such dirs manifest-aware (E-220: only sync-written, unmodified files; user files reported and kept) and never runs implicitly; keep src/agents and src/gemini adapters (D-052). Then run 'ai sync --prune-providers' in this repo to remove .gemini/ and .agents/. Acceptance: sync suite covers mapped-only provisioning, stale report, explicit prune, user-file preservation; this repo has no .gemini/ or .agents/ afterwards and the full suite stays green; mirrors synced. | Tier: 2
  Status: DONE 2026-09-10 — Provider workspaces follow the role binding: mapped-only provisioning, doctor stale report, explicit manifest-aware `ai sync --prune-providers`. .gemini/ and .agents/ removed from th …[full in LOG.md]
- [ ] E-245: Repo hygiene per D-066 §5: git rm the tracked stray root file 'bash' (contains a shell error line, not a script); add .DS_Store and testsprite_tests/tmp/ to .gitignore and remove any tracked/untracked instances; run archive_done_tasks to move DONE rows out of TASKS.md (state.sqlite intact, verify_markdown_sync SYNC_PASS afterwards); confirm git status is clean after 'ai sync'. Acceptance: no stray files at repo root; TASKS.md lists only OPEN/BLOCKED tasks plus the archive pointer; SYNC_PASS; CI green. | Tier: 1
- [ ] E-246: Cut release v3.1.0 per D-066 §5 with skill release-manager: bump package.json, aggregate CHANGELOG.md for D-054..D-066 / E-208..E-246 (same-provider Triad, per-pane role binding, three-channel sovereignty gates, install-first locators, honest CI, ai start, harness hygiene, all-Claude default with fable/opus), create the signed tag, refresh DIGEST. Acceptance: tag v3.1.0 on master, CHANGELOG entry present, CI green, DIGEST version line updated. | Tier: 1

## Archived

Completed tasks beyond the most recent ten are rotated out of this view.
The full record — description, summary and completion date — is in:

- `.ai/archive/state-done-2026-05.json`
- `.ai/archive/state-done-2026-06.json`
- `.ai/archive/state-done-2026-09.json`

`.ai/LOG.md` carries the narrative for every one of them.
