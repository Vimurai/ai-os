description = "Regenerate .ai/DIGEST.md from current project state. Use when DIGEST is stale (> 3 days old or after a major sprint). Reads all .ai/ files and produces a concise 20-60 line snapshot. Replaces the removed `ai digest` shell command (E-34)."
prompt = "Please activate the 'ai-digest' skill and regenerate DIGEST.md."
